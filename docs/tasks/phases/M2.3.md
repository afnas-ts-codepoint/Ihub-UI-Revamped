# M2.3 — Top-bar actions

Status: Completed

Owner: Codex

Reviewer: User-approved on 2026-09-25

Depends On: M2.2

## Objective

Execute the M2.3 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `CommandSearch`: index build, scoring, top 12 results, highlight, keyboard.
  - `ThemeToggle`.
  - Settings link to `/settings/configuration`.
  - `features/notifications` begins: bell data as `LocalizedText`; `NotificationsMenu` with All/Unread, unread dot, and "View all" to `/notifications`.
  - `features/organization` begins: current-user mock and `useCurrentUser`.
  - `Avatar`.
  - Language switch per D8.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

- Golden search parity: for 40 or more EN/AR queries, the new ordered results equal the prototype's `navSearch` output.
  - ↑, ↓, Enter and Escape behave as in the prototype.
  - Bell tabs and counts match.
  - Theme persists per D5.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): D8, D19; D8, D19.

## Prototype Verification

Source: `NAV_SEARCH_INDEX` 3127–3147; `navSearch` 3148–3170; `SearchHighlight` 3171–3183; `SearchBox` 3184–3335; `ThemeToggle` 3336–3345; `NotificationsBell` 3346–3372; `TopBarActions` 3373–3401; `Avatar` 3402–3428.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M2.2

Packages: None

Shared infrastructure: CommandSearch, golden scripts, NotificationsMenu, useCurrentUser, Avatar

Task-card additions: golden-fixture scripts (`scripts/golden/*`).

## Files / Areas Expected

- `src/app/layouts/app-shell/`: `TopBarActions`, `ThemeToggle`, `CommandSearch`, pure navigation index/scoring, shell composition, and interaction tests.
- `src/features/notifications/`: typed `LocalizedText` menu data, query-shaped mock hook, and `NotificationsMenu`.
- `src/features/organization/`: D19 current-user mock and query-shaped `useCurrentUser`.
- `src/shared/ui/avatar/Avatar.tsx`, icon registry additions, and EN/AR top-bar strings.
- `scripts/golden/`: fixed EN/AR query corpus, prototype generator, and generated fixture.
- `tests/visual/m2.3.spec.ts` and `docs/migration/qa/M2.3/`: browser checks and side-by-side evidence.
- Migration task/status, delta, decision, and deviation registers.

## PR

Not opened. All M2.3 changes and evidence are local; no commit or push was performed.

## Review Findings

- Prototype `navSearch` scoring and stable source ordering were ported without normalization, fuzzy matching, or weighting changes.
- The top-level `Masters (List)` search entry remains searchable while its duplicate subtree stays excluded, matching the prototype's `firstLeafRoute` behavior.
- The prototype's malformed `T(...)` calls leave two notification subjects in English under Arabic; those visible values remain English for parity instead of being silently fixed.
- Search is hidden at 760px and 390px exactly as the prototype's `.hide-sm` rule requires. The other actions remain on one compact row; D8 adds only the small language label beside the theme toggle.
- Existing `/notifications` and `/settings/configuration` `MigrationPending` destinations remain unchanged.

## Deviations

D8 is the approved intentional visible difference: the production language switch appears next to `ThemeToggle`. D20's Western/Latin digits are also preserved in Arabic notification copy. No unapproved architecture deviation was introduced.

## Blockers

No implementation blocker. Remote CI/PR execution was not run because no PR/commit was requested.

## Human Approval

Approved by the user on 2026-09-25. M3.1 remains pending and has not started.

## Completion Notes

### Phase and prototype intake

- Phase: M2.3 — Top-bar actions.
- Prototype ranges reviewed: `NAV_SEARCH_INDEX` 3127-3147; `navSearch` 3148-3170; `SearchHighlight` 3171-3183; `SearchBox` 3184-3335; `ThemeToggle` 3336-3345; `NotificationsBell` 3346-3372; `TopBarActions` 3373-3401; `Avatar` 3402-3428.
- R6 result: no relevant source delta from baseline `273abc8` to prototype HEAD `3c391b4`.

### Delivered scope

- `CommandSearch` builds its index from the approved M2.1 `NAV_TREE`; no second navigation catalogue exists. The pure scorer reproduces exact/prefix/word/substring/parent scoring, leaf and depth adjustments, stable source order, and the top-12 cap.
- `SearchHighlight` preserves matched and unmatched text in EN/AR. Arrow Up/Down wrap, Enter navigates the selected result, Escape clears/closes, selection resets with the query, outside click closes, and Ctrl/Cmd+K opens as in the prototype.
- Golden infrastructure generated 48 fixed cases from the running read-only prototype: 24 English and 24 Arabic, including exact/partial, nested, Masters, low-result, and no-result queries. Ordered production keys equal the prototype fixture for every case.
- `ThemeToggle` uses the existing M1.3 preference store and `<html data-theme>` subscription. Browser checks pass for Paper → Ink → reload and Ink → Paper → reload; the M2.2 theme-aware logo continues to follow the same attribute.
- D8 language switching uses the existing locale preference only. EN → AR and AR → EN update i18next, `<html lang>`, `<html dir>`, shell direction, and labels without reload, and both directions persist across reload.
- `NotificationsMenu` uses eight prototype rows and six unread rows, shows the unread bell dot, filters All/Unread, localizes visible data according to parity and D20, closes from its controls/outside click, and routes View all to `/notifications`.
- `useCurrentUser` returns a query-shaped result from the local D19 mock. `Avatar` deterministically renders `AA` for Ahmad Al Osaimi at 32px with theme/RTL-safe placement; no profile menu or authentication layer was added.
- Settings routes through the shared path contract to `/settings/configuration` and retains the existing M2.1 pending destination.
- No package was added or changed. No backend, API, authentication, fuzzy-search, notification persistence, or M3.1 infrastructure was introduced.

### Responsive, RTL, and visual QA

- Full evidence matrix: five states × four widths × two themes × two locales = 80 side-by-side captures.
- States: default/closed top bar; CommandSearch results; keyboard-selected search result; notifications All; notifications Unread. Search-open states are captured at 1440/1040 and intentionally remain hidden at 760/390 to verify prototype-responsive behavior.
- Evidence: `docs/migration/qa/M2.3/<state>/<width>-<theme>-<locale>.png`.
- RTL verification covers action order, inline-end overlays, search text/highlighting, localized labels, menu rows, and shell direction. The compact 390px header retains burger, wordmark, theme, language, Settings, notifications, and avatar without wrapping.
- Reviewed visible differences: D8 language control and D20 Latin digits are intentional. Later-phase screen content remains represented by existing `MigrationPending` pages and is unrelated to M2.3. No open material top-bar mismatch was found.

### Quality gates

- G1: pass — `npm run typecheck`, 0 errors.
- G2: pass — `npm run lint`, 0 errors/warnings.
- G3: pass — `npm run test`, 22 files and 153 tests.
- G4: pass — `npm run build`; `npm run check:bundle` inspected 9 text bundle files. Vite retains the existing non-failing >500kB chunk advisory.
- G5: pass within the full test run — EN/AR key parity remains green.
- G6: pass within the full test run — navigation/route parity and browser/hash router regression coverage remain green.
- G7: pass — 80 side-by-side captures plus persistence/responsive browser checks; the 16 final Arabic notification cells were regenerated after the parity correction.
- G8: pass — every new export has an M2.3 consumer; no speculative API, profile menu, notification screen, or next-phase code was created.
- G9: pass — dependency manifests contain no package addition; only task scripts were added.
- G10: pass — delta, decision, deviation, NOOP, pending, and task registers were reviewed. No new NOOP or `MigrationPending` marker applies; `npm run report:pending` still reports the two expected shared M2.1 source markers.
- Golden parity: pass — all 48 ordered EN/AR fixtures match the running prototype.
- M1.x, M2.1, and M2.2 regression tests: pass within the 153-test suite.
- Playwright: 80/80 matrix captures passed; persistence/responsive smoke passed; 16/16 final affected Arabic notification cells passed regeneration.

## User-visible behavior

1. Users now see Command Search, Paper/Ink theme, EN/AR language, Settings, notifications, and the Ahmad Al Osaimi avatar in the top bar.
2. Command Search opens from its icon or Ctrl/Cmd+K, searches the shared navigation in prototype order, highlights direct matches, supports Arrow Up/Down, opens with Enter, and closes/clears with Escape or outside click.
3. Paper/Ink changes immediately, updates `<html data-theme>`, preserves the logo treatment, and survives reload in either direction.
4. EN/AR changes immediately without reload, updates text, `lang`, `dir`, shell mirroring, and persists in either direction while retaining Latin digits.
5. The notification bell shows an unread dot and a menu with eight All rows, six Unread rows, localized content, and View all navigation.
6. The top bar displays the approved local prototype user Ahmad Al Osaimi as the deterministic `AA` avatar.
7. Settings navigates to `/settings/configuration`, whose existing migration-pending behavior remains in place.
8. At 760px and 390px Command Search is hidden as in the prototype; the remaining controls stay in the compact responsive header.
9. The visible EN/AR switch beside the theme toggle is the approved intentional D8 difference from the prototype.
10. No unrelated application screen changed; later-phase routes retain their approved M2.1 pending/placeholder behavior.

**M3.1 has not started.**

