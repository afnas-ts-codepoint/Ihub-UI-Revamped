# M3.5 — Quality & Compliance: Checklist

Status: Completed

Owner: Codex

Reviewer: User-approved on 2026-09-26

Depends On: M3.2 (approved 2026-09-26)

## Objective

Migrate the current-prototype Checklist screen to `/quality`, reuse the approved M3.1/M3.2 infrastructure, preserve D14 child placeholders, run M3.5 quality gates and visual QA, then stop for human approval.

## Prototype sources inspected

- `ChecklistScreen`: `index.html` lines 8570-8664.
- `RecordFilter` and `RF_KIND.sheet`: lines 1416-1727.
- `TabbedTable` and inert pagination: lines 8070-8227.
- `STATUS_PILL`: lines 8271-8284.
- `PageHeader`: lines 3429-3479.
- Quality navigation: lines 2526-2540.
- App route switch and `StubScreen` fallback: lines 22472-22528.

## Delta intake result

Baseline `273abc8` was compared with current prototype HEAD `3c391b4`. `git diff 273abc8..HEAD -- index.html` is empty, so the Checklist screen and directly used dependencies are unchanged. No prototype delta required an adopt/defer decision. The result is recorded in `docs/migration/PROTOTYPE_DELTAS.md`.

## Plan/prototype consistency result

Direct source inspection confirmed all four required assumptions:

1. The prototype `checklist` route is the Quality & Compliance root and maps to production `/quality`.
2. `ChecklistScreen` defines exactly two tabs.
3. It renders `RecordFilter` with `kind="sheet"`.
4. Quality child nodes without a real route fall through to `StubScreen`; production keeps them as `PlaceholderPage` under D14.

No material phase-card conflict was found, so no human reconciliation decision was needed before implementation.

## Scope implemented

- Added `src/features/checklists/` with typed data, tab definitions, status mapping, a data hook, `ChecklistTable`, `ChecklistPage`, tests, and a public API.
- Replaced only the `/quality` index route's `NavRoutePage`/`MigrationPending` behavior with `ChecklistPage`.
- Removed `routeBehavior: 'migration-pending'` from only the Quality root nav node.
- Reused `SectionLayout`, `RecordFilter kind="sheet"`, `TabbedTable`, `TablePagination`, `EmptyState`, and `Chip` without new shared exports.
- Added the `checklists` EN/AR namespace and typed i18n registration.
- Added focused Checklist, route, D14 placeholder, i18n, filter, table, and visual-QA coverage.
- Added no runtime or development dependency.

## Files and folders changed

- `src/features/checklists/`: page, table component, mock data, status domain map, hook, types, public API, and tests.
- `src/shared/i18n/locales/{en,ar}/checklists.json`, `src/shared/i18n/i18n.ts`, `src/app/i18n.d.ts`, and the parity test.
- `src/app/router/router.tsx`, `src/app/navigation/nav.config.ts`, SectionLayout/navigation parity tests, and `quality-placeholders.test.tsx`.
- `tests/visual/m3.5.spec.ts`, `package.json`, and `docs/migration/qa/M3.5/`.
- M3.5 task tracking and the delta, NOOP, pending, and decision registers.

## `/quality` route and SectionLayout

- `/quality` now renders `ChecklistPage` inside the existing M3.1 `SectionLayout`.
- `/quality?view=report` renders the existing Checklist report through `reportKey: 'checklist'`.
- Section/Report switching, direct report URLs, and browser routing remain owned by the unchanged `SectionLayout`.
- `/quality/sla` remains `MigrationPending` for M3.8.
- No SectionLayout production code changed.

## Exact tabs and counts

The tabs are in prototype order:

| ID | Label | Count | Behavior |
| --- | --- | --- | --- |
| `unapproved` | Unapproved Checklist | 4 | Active by default; literal count. |
| `other` | Other Checklist | 23 | Literal count. |

Both counts are source literals, stored as `displayCount` values rather than derived from rows. Switching tabs changes only the active presentation and continues to show the same four rows, exactly as the prototype does. This is registered as `PROTOTYPE-NOOP(D2)`.

## Checklist rows and columns

Columns match the prototype in order: Checklist #, Title, Site, Submitted, Submitted by, Progress, Status.

Rows match exactly:

1. `CHK-0421` — Daily safety walk — Mall floor 1; SAMA Mall; Today, 8:12 AM; K. Ibrahim; 24/24; Awaiting.
2. `CHK-0420` — Cleaning round — Food court; Riyadh Park; Today, 7:48 AM; M. Hassan; 18/20; Awaiting.
3. `CHK-0419` — Fire drill verification; SAMA Mall; Apr 28; O. Najjar; 12/12; Approved.
4. `CHK-0418` — Vendor compliance audit; Tower Plaza; Apr 28; L. Haddad; 15/16; Approved.

Progress uses the existing `.num` presentation and end alignment. The table remains horizontally scrollable at smaller widths.

## Status and tone mapping

Checklist owns its mapping and reuses shared `Chip` presentation:

- Awaiting → `warn`.
- Approved → `ok`.

Tests assert both mappings against representative rows.

## RecordFilter configuration

- Reuses M3.1 `RecordFilter` with `kind="sheet"`; no Checklist-specific filter was created.
- The sheet search placeholder begins with `AS-2026-114` and the dialog uses `Action sheet number` plus the sheet categories, subcategories, and status options already defined in organization-owned filter vocabulary.
- Existing behavior is preserved for quick flags, searchable/multi-select fields, applied chips, individual chip removal, Clear All, date range min/max linkage, export, EN/AR, and RTL.
- M3.5 page coverage verifies the sheet identity, dialog groups, applying/removing a search chip, Clear All, and Arabic chrome. Existing M3.1 regression coverage verifies every filter kind plus date constraints and field interactions.

## Shared-table reuse and NOOPs

- `ChecklistTable` composes the existing `TabbedTable`; no table framework or parallel table system was introduced.
- `EmptyState` remains available through the shared table for a zero-row input; the prototype's default four-row dataset does not reach it.
- Previous, pages 1-3, and next remain visible and inert through the existing `TablePagination`, registered for M3.5 as `PROTOTYPE-NOOP(D2)`.
- The prototype's `exportable: true` prop renders no table-level control (`exportable && null`); production likewise adds no speculative table export control. Export remains available through the reused RecordFilter bar.

## Quality child placeholders — D14

Dedicated route tests verify these remain `PlaceholderPage`:

- `/quality/dashboard`.
- `/quality/observations`.
- `/quality/quality-assurance-checklists`.
- Its eight descendants: risk levels, standard parameters, zone accountability, standards, create checklist, fill checklist, edit filled checklist, and report.

`/quality/sla` remains a distinct `MigrationPending` route for M3.8, not a placeholder and not part of M3.5. D14 is re-confirmed in `DECISIONS.md`.

## EN/AR, RTL, and responsive result

- EN/AR Checklist resource key sets match.
- English renders the prototype title emphasis (`SOP` + italic magenta `Checklist`); Arabic renders the prototype's plain `قائمة إجراءات التشغيل` title.
- The prototype leaves tab labels, columns, statuses, and row content in English in Arabic mode; production preserves that content through the Arabic resource rather than hardcoded JSX.
- Existing RecordFilter and pagination chrome localize in Arabic. RTL uses existing logical alignment and mirrors the page, tabs, filter, table, progress/status cells, and pagination. Western digits remain per D20.
- At 760px and 390px, tabs and the table retain their prototype form and scroll horizontally; no mobile card transformation was invented.

## Visual evidence

`npm.cmd run visual:capture:m3.5` passed 25/25 captures across five states and the approved reduced variants.

States: Section/default first tab; Other Checklist second tab; RecordFilter dialog; applied search filter; Report view.

Variants per state: 1440 Paper EN, 1440 Ink EN, 1440 Paper AR, 760 Paper EN, and 390 Paper EN.

Evidence is stored under `docs/migration/qa/M3.5/<state>/<width>-<theme>-<locale>.png`. The report subset was regenerated after adding an explicit route-render wait to the harness. Side-by-side review found no open material M3.5 difference.

## Dependency diff

No dependency or lockfile change. `package.json` adds only `visual:capture:m3.5`. No Axios, query/form/table/grid/chart/PDF dependency was added.

## Quality gates

- G1 typecheck: pass — `npm.cmd run typecheck`, 0 errors.
- G2 lint: pass — `npm.cmd run lint`, 0 errors/warnings.
- G3 tests: pass — focused M3.5 set 4 files/39 tests; full suite 40 files/255 tests.
- G4 build/bundle: pass — `npm.cmd run build`; only the pre-existing >500 kB advisory. `npm.cmd run check:bundle` passed, 21 files inspected.
- G5 i18n parity: pass — `checklists` is included in the active EN/AR parity table.
- G6 nav↔route parity: pass — all 271 nav nodes still resolve; the Quality root heading override reflects the prototype's nav-label/screen-title difference.
- G7 fidelity evidence: pass — 25/25 reduced-matrix captures generated and reviewed; no open material difference.
- G8 no speculative code: pass — all Checklist exports have an M3.5 consumer; no new shared export, filter implementation, table system, or future child page was added.
- G9 dependency diff: pass — no dependency or lockfile change.
- G10 registers: pass — delta, NOOP, pending, and D14 decision registers updated; deviations remain unchanged.

## Regression results

- Full Vitest suite: 40 files, 255 tests passed, including approved M1/M2/M3.1-M3.4 coverage.
- Checklist-focused/route set: 4 files, 39 tests passed.
- `npm.cmd run report:pending`: passed with the same two shared future-phase source markers (`MasterRoutePage.tsx`, `NavRoutePage.tsx`).
- `npm.cmd run build` and `npm.cmd run check:bundle`: passed.
- `git diff --check`: passed; only repository line-ending notices were emitted.

## Register changes

- `PROTOTYPE_DELTAS.md`: recorded the clean M3.5 intake and consistency result.
- `PROTOTYPE_NOOPS.md`: registered literal Checklist tab counts/shared-row switching and inert Checklist pagination.
- `MIGRATION_PENDING.md`: removed `/quality` only; retained `/quality/sla` for M3.8.
- `DECISIONS.md`: re-confirmed D14 for Quality child placeholders.
- `DEVIATIONS.md`: unchanged; no architecture or fidelity deviation was introduced.

## Deviations and blockers

Deviations: None.

Blockers: None. Remote CI/PR execution was not run because no commit, push, or PR was requested.

## User-visible behavior

1. `/quality` now displays the real SOP Checklist section instead of `MigrationPending`.
2. The two visible tabs are Unapproved Checklist (4) and Other Checklist (23), in that order.
3. Both tabs show the same four prototype rows; switching changes only the active tab, matching the prototype.
4. Users can search and use the existing `sheet` RecordFilter fields, quick flags, applied chips, chip removal, Clear All, date range, and export controls.
5. The rows show CHK-0421 through CHK-0418 with their exact titles, sites, submitted values, submitters, progress, and Awaiting/Approved tones.
6. Tab counts and pagination are intentionally inert/literal under `PROTOTYPE-NOOP(D2)`.
7. Screenless Quality child routes remain `PlaceholderPage`; `/quality/sla` remains pending for M3.8.
8. Arabic renders the prototype Arabic title, localized filter chrome, RTL mirroring, and Western digits while retaining the prototype's English table vocabulary.
9. Mobile widths keep the tab/table presentation with horizontal overflow rather than inventing cards.
10. No unrelated screen changed.

## PR

Not opened. No commit or push was performed.

## Human approval

M3.5 implementation, tests, visual QA, NOOP registration, quality gates, and register updates were approved by the user on 2026-09-26.

**M3.6 has not started.**
