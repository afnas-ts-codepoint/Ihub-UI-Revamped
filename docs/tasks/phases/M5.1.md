# M5.1 — Home layout and banner

Status: Pending

Owner: Not assigned

Reviewer: Not assigned

Depends On: M3.5, M3.8

## Objective

Execute the M5.1 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `features/home` begins: `HomeLayout`, `HomeBannerLayout`, `HomeTopBanner`.
  - The banner includes:
    - greeting and a profile clock fixed to Asia/Bahrain;
    - a 4-slide carousel (6s auto-advance, pause on hover or focus, dots, next);
    - the AI subscription menu;
    - the pulse strip;
    - the tab bar with counts.
  - Home datasets with read hooks and count derivation (no store yet).
  - `/home/*` tab routes render `MigrationPending`, except **SOP Checklist** (M3.5) and **SLA** (M3.8), which go live.
  - The incidents sub-tab switch.
  - Task routes wrapped in `HomeBannerLayout`.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

full matrix for the banner (each slide and the paused state); tab counts equal the prototype; clock correct under a fixed time; carousel timing tested with fake timers.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): delta intake

## Prototype Verification

Source: `HomeTopBanner` 14983–15161; `HeroCarousel` 14899–14976 (**volatile**); `PulseStrip` 12678–12783; `TabBar` 12521–12574; `AiSubscriptionMenu` 5819–5853; OCC datasets 12130–12459; `ProgressRing` 1817–1871; incidents sub-tab switch 14746–14778.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M3.5, M3.8

Packages: None

Shared infrastructure: ProgressRing, useNow

Task-card additions: `ProgressRing`, `useNow`. No chart library is needed here (verified: the banner uses only `ProgressRing`).

## Files / Areas Expected

TBD at task start from the approved scope; do not create speculative files or empty folders.

## PR

TBD

## Review Findings

TBD

## Deviations

TBD

## Blockers

TBD

## Human Approval

Required

## Completion Notes

TBD

