# M4.1 — Master listing

Status: Pending

Owner: Not assigned

Reviewer: Not assigned

Depends On: M3.2

## Objective

Execute the M4.1 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- Definition-driven `MasterPage` for the five `MASTERS_WITH_PAGE` items:
    - search, status chips and an in-row status toggle;
    - a settings dialog for column, chip and filter-field visibility (Select all / Deselect all);
    - 10 rows per page, "Showing X of Y records" and pagination;
    - row actions;
    - the Export button, verified at phase start and inert by default;
    - a mode-specific filter dialog;
    - delete confirmation.
  - `MasterPendingPage` for every other item.
  - The Masters (List) variant per D10.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

per-mode columns and filters match (PC, AA, SA, TM, generic); pagination and visibility behave identically; delete text matches; reduced matrix for each of the five masters.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): D10

## Prototype Verification

Source: `MasterListingMock` 4520–5020; `MasterFilterModal` 3624–3731; `MasterPagePending` 4241–4265; `MasterDeleteModal` 4492–4519; seed rows 3489–3623.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M3.2

Packages: @tanstack/react-table

Shared infrastructure: table engine, DataTableView, TablePagination (functional), ColumnSettingsDialog, RowActions, TableToolbar, ConfirmDialog

Task-card additions: table engine, `DataTableView`, functional `TablePagination`, `ColumnSettingsDialog`, `RowActions`, `TableToolbar`, `ConfirmDialog`.

## Files / Areas Expected

TBD at task start from the approved scope; do not create speculative files or empty folders.

## PR

TBD

## Review Findings

TBD

## Deviations

TBD

## Blockers

TBD

## Human Approval

Required

## Completion Notes

TBD

