# M3.7 — Workflows

Status: Completed

Owner: Codex

Reviewer: User-approved on 2026-09-26

Depends On: M3.1

## Objective

Execute the M3.7 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `features/workflows` with 4 tabs.
  - A rules table with expandable rows, built on plain primitives.
  - `Switch`, the KPI strip and the step timeline.
  - SLA & escalation table with the derived "next owner / Department Head" rule.
  - New Rule, rule/step Edit, Discard, Save Draft and Publish stay inert and registered.
  - The version badge.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

Parity on every tab; `durMins`, `fmtTotal` and escalation logic unit-tested against values the prototype renders.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.

## Prototype Verification

Source: Workflows block 21190–21528 plus directly used route, Section/Report, navigation, theme, and locale behavior.

- Prototype HEAD: `3c391b4`.
- Approved baseline: `273abc8`.
- Delta intake: `index.html` is unchanged between the approved baseline and current prototype HEAD.
- Consistency check: confirmed four ordered tabs, routing table and expansion content, decorative Switch, KPI strip, step timeline, SLA/escalation table, next-owner/Department Head derivation, literal published `v3.2` badge, and inert New Rule/Edit/Discard/Save Draft/Publish controls.
- No material plan/prototype conflict was found; no adopt/defer decision was required.

## Dependencies / Packages

- Dependencies: M3.1.
- Packages added or changed: none.
- Shared infrastructure: one domain-agnostic decorative `Switch`, immediately consumed by Workflows.
- `package.json` adds only the `visual:capture:m3.7` script; `package-lock.json` is unchanged.

## Files / Areas Changed

- `src/features/workflows/`: typed fixtures, feature types, pure duration/escalation domain functions, four-tab composition, rules/flow/auto-routing/SLA components, page, public API, and tests.
- `src/shared/form/controls/Switch.tsx`: first approved consumer-backed shared Switch plus its unit test.
- `src/shared/i18n/locales/{en,ar}/workflows.json`, i18n registration/types, and parity coverage.
- `src/app/router/router.tsx` and `src/app/navigation/nav.config.ts`: `/workflows` now resolves to the real feature and no longer carries a pending navigation marker.
- Route/nav parity tests and the Workflows Section/Report route test.
- `tests/visual/m3.7.spec.ts`, `package.json`, and 25 side-by-side captures under `docs/migration/qa/M3.7/`.
- M3.7 delta, NOOP, pending, and phase registers.

## Four Tabs and Screen Behavior

- Exact order and labels: Routing Rules; Operational Flow; Auto-Routing · TX; SLA & Escalation.
- Routing Rules is initially active. Tabs switch local content without navigation or fabricated counts/badges; the prototype has none.
- Department and Division selectors preserve all six departments, their divisions, and department-specific task/rule data. Changing department resets Division and task selection to that department's first configured values, matching the prototype.
- The literal green published badge reads `Published workflow · v3.2` (localized in Arabic) and is not derived from package metadata.

## Rules Table and Expansion

- All prototype columns, department-specific rows, labels, conditions, destinations, SLA values, active states, and ordering are preserved.
- The first rule starts expanded. Clicking any rule toggles that row and closes the prior row. Expansion remains inline and displays the matching task's numbered action, owner, and duration sequence.
- Expansion keeps the prototype's pointer-row behavior: rows are not converted into focusable buttons, while the nested native Edit button retains its own focus behavior and does not trigger expansion.
- Rules without a matching operational task show the prototype empty-flow message.
- Rule Edit stops the row click but performs no edit. No dialog, sorting, filtering, or TanStack Table was introduced.

## Switch

- The shared `Switch` is domain-agnostic and has only a checked/unchecked visual state.
- It remains decorative and non-focusable because the prototype `Toggle` is a span with no interaction.
- Routing Rules visibly covers both checked and unchecked states; Auto-Routing renders its seven checked states. LTR/RTL thumb placement, Paper/Ink colors, and the explicit white thumb were visually verified.

## KPI Strip and Step Timeline

- Operational Flow preserves the prototype task chips, order, action/owner/duration cards, numbered steps, connectors, and inert per-step Edit icons.
- KPI values are derived from the selected task using the prototype rules. The default Guest Complaint shows 5 Steps, `3 h` Target end-to-end, and 3 Owners.
- The workflow-specific timeline remains local to the feature; no speculative generic timeline system was added.

## SLA / Escalation and Domain Logic

- The SLA table preserves Step, Owner, Target Time, Share of total, and On breach escalation columns, task chips, row order, duration bars, and warning tone.
- `durMins` ports the exact 8-hour workday conversion for `m`, `h`, `d`, and em-dash/empty durations.
- `fmtTotal` ports the exact minute/hour/day thresholds and one-decimal rounding visible in the prototype.
- `escalationTarget` is a pure domain rule: all non-final steps produce the next numbered step owner, and the final reachable step produces Department Head.
- Domain tests cover rendered `5m`, `30m`, `1h`, `2h`, `1d`, `5d`, zero/em-dash, minute/hour/day total formatting, normal owner progression, final-step Department Head, and the one-step boundary.

## Inert Actions / NOOP Registration

- `PROTOTYPE-NOOP(D2)` entries were added for New Rule, rule and step Edit, Discard, Save Draft, and Publish.
- Tests click Discard, Save Draft, Publish, New Rule, a rule Edit control, and a step Edit control and confirm that no workflow state or rendered content changes.
- The controls remain visible and styled like the prototype; they were not made functional, removed, or newly disabled.

## EN/AR, RTL, Responsive, and Section/Report

- Added one Workflows namespace with EN/AR key parity and no user-visible JSX literals.
- Prototype business data that remains English in both locales (owners, actions, routes, and conditions) remains literal data; department, division, task/category names, UI labels, headings, and messages localize.
- Western digits are retained. Arabic mirrors directional arrows, switch thumbs, timeline placement, and table flow.
- Desktop, 760px, and 390px retain prototype wrapping and horizontal overflow. Tabs, rules/expansion, action controls, cards, KPI strip, timeline, and SLA table remain usable without an invented mobile transformation.
- Existing M3.1 Section/Report behavior is reused. `/workflows?view=report` renders the approved Workflows report and returning to Section restores the four-tab screen.

## Visual Evidence

- `npm.cmd run visual:capture:m3.7`: 26/26 passed.
- Direct behavior evidence compares the four tab labels and the full default rules/expanded-row table matrix against the running prototype.
- Reduced matrix: 25 side-by-side captures (Routing Rules expanded, Operational Flow/KPI/timeline, Auto-Routing/Switches, SLA/escalation, and Report).
- Variants per state: 1440 Paper EN, 1440 Ink EN, 1440 Paper AR, 760 Paper EN, and 390 Paper EN.
- Spot review covered blue primary actions, initial/clicked tab treatment, checked/unchecked switch thumbs, expanded-row density, KPI/timeline, Auto-Routing cards, SLA wrapping/bars/escalation, Paper/Ink, Arabic/RTL, and narrow horizontal overflow. The identified tone, tab, thumb, chip-border, and SLA-wrap differences were corrected and all captures regenerated.

## Quality-Gate Results

- G1 typecheck: pass — `npm.cmd run typecheck`, 0 errors; the production build repeated TypeScript validation successfully.
- G2 lint: pass — final `npm.cmd run lint`, 0 errors/warnings.
- G3 tests: pass — full suite 50 files, 403 tests; final focused Workflows page suite 1 file, 5 tests.
- G4 build/bundle: pass — production build completed; only the pre-existing >500 kB advisory. `check:bundle` passed with 25 files inspected.
- G5 i18n parity: pass — Workflows EN/AR resources are registered, typed, loaded, and included in the active parity test.
- G6 nav↔route parity: pass — `/workflows` renders the real screen, remains under SectionLayout, and no longer resolves through `MigrationPending`.
- G7 fidelity evidence: pass — 1 direct prototype behavior comparison plus 25/25 regenerated reduced-matrix captures; no open material M3.7 mismatch remains.
- G8 no speculative code: pass — every new feature/shared export has an immediate M3.7 consumer; no store, API layer, generic timeline/table system, or unsupported behavior was added.
- G9 dependency diff: pass — no dependency or lockfile change.
- G10 registers: pass — delta, NOOP, MigrationPending, task status, and phase report updated; `report:pending` retains exactly the two expected shared future-phase markers in `MasterRoutePage.tsx` and `NavRoutePage.tsx`.

## Regression Results

- Full Vitest suite: 50 files and 403 tests passed, including all approved M1/M2/M3.1-M3.6 coverage.
- Production build and 25-file bundle inspection passed.
- `report:pending` reports exactly two expected shared future-phase implementation markers.
- `git diff --check` passed; line-ending notices only.
- No unrelated route changed. The prototype repository was not modified.

## Register Updates

- `PROTOTYPE_DELTAS.md`: recorded the no-delta M3.7 intake and consistency result.
- `PROTOTYPE_NOOPS.md`: registered New Rule, rule/step Edit, Discard, Save Draft, and Publish as active `PROTOTYPE-NOOP(D2)` behavior.
- `MIGRATION_PENDING.md`: changed only Workflows to Removed/completed; future-phase markers, including M3.8 SLA & Compliance, remain active.
- `DECISIONS.md` and architecture deviation registers: unchanged; no decision or deviation was required.

## Deviations

None. No unapproved architecture, dependency, or prototype-behavior deviation was introduced.

## Blockers

None. Remote CI/PR execution was not run because no commit, push, or PR was requested.

## PR

Not opened. No commit or push was performed.

## Human Approval

M3.7 was approved by the user on 2026-09-26.

## User-visible behavior

1. `/workflows` now shows the real prototype-faithful Workflows screen instead of `MigrationPending`.
2. Users see the four ordered tabs: Routing Rules, Operational Flow, Auto-Routing · TX, and SLA & Escalation.
3. The first routing rule begins expanded; clicking a row toggles its inline operational steps, while Edit remains inert and does not toggle the row.
4. The KPI strip shows selected-task Steps, Target end-to-end, and unique Owners; Guest Complaint defaults to 5, `3 h`, and 3.
5. The step timeline shows numbered actions in order with owner, duration, connector, and inert Edit control.
6. The SLA/escalation table shows each step's owner, target, relative-duration bar, next-step owner, and Department Head for the final step.
7. New Rule, rule/step Edit, Discard, Save Draft, and Publish intentionally remain visible and inert.
8. The green version badge displays the literal published workflow version `v3.2` (localized in Arabic).
9. EN/AR, RTL, Paper/Ink, desktop/tablet/mobile, and horizontal-overflow behavior were verified against the reduced visual matrix.
10. No unrelated route changed.

**M3.8 has not started.**
