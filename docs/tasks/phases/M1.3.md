# M1.3 — i18n, preferences, direction

Status: Completed

Owner: Codex

Reviewer: User

Depends On: M1.2

## Objective

Execute the M1.3 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `shared/i18n`:
    - i18next instance with a lazy namespace backend (`import.meta.glob`);
    - `common` and `validation` namespaces (only the keys used so far);
    - `format.ts` (KWD with 3 decimals, digits per D20, Asia/Bahrain helper);
    - `calendar.ts` (explicit month and weekday resources);
    - `localized.ts` (`LocalizedText`) and `useDirection`.
  - `app/i18n.d.ts` for typed keys.
  - `store/preferences.store.ts`: theme and locale; persistence per D5 through a try/catch storage adapter under key `ihub.v2.preferences`.
  - `app/bootstrap.ts`: applies `lang`, `dir` and `data-theme` to `<html>` before first render and syncs i18next.
  - A dev-only `?lng=` switch (removed from production builds) until D8 is implemented.
  - `scripts/extract-strings`: turns `T('en','ar')` pairs from a prototype line range into draft namespace JSON.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

- The en/ar parity test fails on a missing key.
  - Formatter tests pass: `1,234.500 KWD`, Latin digits in Arabic, Asia/Bahrain time.
  - Switching locale flips `dir` and `lang` without a reload.
  - The production bundle has no `?lng` handler.
  - The extraction script reproduces a sample range verbatim.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): D5, D20; D5, D20.

## Prototype Verification

Source: `T` helper 2614; locale and theme effects 22379–22388; date-picker labels 1526–1529; profile clock fixed to Asia/Bahrain (~15001).

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M1.2

Packages: i18next, react-i18next, zustand

Shared infrastructure: shared/i18n, preferences store, bootstrap, string-extraction script

## Files / Areas Expected

- `src/shared/i18n/` instance, locale resources, formatters, calendar labels, localized-content helpers, direction helper, and tests.
- `src/app/i18n.d.ts` and `src/app/bootstrap.ts` with bootstrap tests.
- `src/store/preferences.store.ts` with persistence tests.
- `scripts/extract-strings.mjs` and its sample-range test.
- Package, bundle-check, tracker, delta, and decision-register updates required by M1.3.

## PR

Not opened. No commit or push was performed; remote CI therefore remains pending until an authorized PR exists.

## Review Findings

- The lazy i18next backend discovers locale namespace JSON through `import.meta.glob`; `common` loads at bootstrap and later namespaces remain on-demand chunks.
- The current resources contain only M1.3 calendar and duration keys. The required `validation` namespace is intentionally empty until its first validation consumer.
- The preferences store validates hydrated values and catches storage failures while persisting theme and locale under `ihub.v2.preferences`.
- Bootstrap applies `lang`, `dir`, and `data-theme` to `<html>` before React renders, then subscribes to preference changes and synchronizes i18next.
- The development `?lng=en|ar` verifier has no rendered control and its marker is forbidden by the production bundle check.
- The extraction CLI writes paired en/ar draft namespace JSON from a requested prototype line range and preserves the extracted values.

## User-Visible Behavior

- No screen, shell control, language selector, menu item, or other production UI was added.
- In development only, loading with `?lng=ar` applies Arabic `lang`, RTL `dir`, and the M1.2 Arabic font stack without a reload; `?lng=en` restores English/LTR.
- Locale and Paper/Ink theme values persist across reloads through `ihub.v2.preferences`.
- The formatting infrastructure produces `1,234.500 KWD`, Latin digits for Arabic, and profile-clock time in `Asia/Bahrain`; no screen consumes these formatters yet.
- The calendar infrastructure retains the prototype's explicit Arabic month names and two-letter weekday labels.

## Deviations

None.

## Blockers

No implementation blocker. Remote CI remains unexecuted until a future authorized commit/PR.

## Human Approval

Approved on 2026-09-25. The user confirmed verification.

## Completion Notes

- Delivered: lazy typed en/ar i18n, `common`/`validation` namespaces, format/calendar/localized/direction helpers, persisted preferences, pre-render bootstrap, development-only `?lng=`, and the string-extraction CLI.
- Packages added: `i18next@26.4.2`, `react-i18next@17.0.15`, `zustand@5.0.15`.
- R6: no prototype `index.html` delta from baseline `273abc8` to current HEAD `3c391b4`, including all M1.3 source ranges.
- G1: pass, `npm run typecheck` (0 errors).
- G2: pass, `npm run lint` (0 errors).
- G3: pass, `npm run test` (7 files, 24 tests).
- G4: pass, `npm run build` and `npm run check:bundle` (production build complete; 7 text bundle files inspected; no development locale marker).
- G5: pass; the en/ar parity test covers `common` and `validation` and fails on a missing leaf key.
- G6–G7: not active at M1.3; no screen or route was introduced.
- G8: pass; every new shared export is consumed by M1.3 bootstrap, tests, or the required helper API. No future feature files were created.
- G9: pass; only the three packages authorized by the M1.3 card were added.
- G10: pass; the delta and decision registers were updated. No NOOP, pending, or deviation entry was applicable.
- Extraction acceptance: the unit sample and prototype line 15002 reproduced both source strings verbatim in paired draft JSON.
- Open questions: none. Human approval is requested before M1.4.

