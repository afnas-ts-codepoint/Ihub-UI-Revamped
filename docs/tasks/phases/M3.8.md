# M3.8 — SLA & Compliance

Status: Pending

Owner: Not assigned

Reviewer: Not assigned

Depends On: M3.1

## Objective

Execute the M3.8 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `features/sla` at `/quality/sla`.
  - Overview: clock-rules card, department table, and an item drill-in dialog with an inert preview and a time-used progress bar.
  - Work-area mapping: search, priority filter, and an add/edit/remove dialog.
  - Public hooks `useSlaLevels` and `useSlaPerformance`.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

parity; on-time % and the save/default rules unit-tested; no error text appears where the prototype shows none.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): None beyond the applicable standard gates and human approval.

## Prototype Verification

Source: SLA block 21660–22070 (`saveEdit` at 21820).

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M3.1

Packages: react-hook-form, @hookform/resolvers, yup

Shared infrastructure: ProgressBar, RHF adapters, TextInput, TextArea, yupLocale, requiredText

Task-card additions: `ProgressBar`; React Hook Form + Yup, with `shared/form/rhf` adapters, `TextInput`, `TextArea`, `yupLocale` and `requiredText`.

## Files / Areas Expected

TBD at task start from the approved scope; do not create speculative files or empty folders.

## PR

TBD

## Review Findings

TBD

## Deviations

TBD

## Blockers

TBD

## Human Approval

Required

## Completion Notes

TBD

