# Migration Pending

Track prototype functionality that has not yet been migrated.

| Feature | Route/screen | Migration phase | Status | Notes |
| --- | --- | --- | --- | --- |
| Migration infrastructure | Not routed | M1.4 | Available; 0 active markers | `MigrationPending` and `report:pending` are introduced for later route migrations. Each marker is removed by its owning migration phase; the count must be zero before release. |
