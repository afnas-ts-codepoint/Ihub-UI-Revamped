# M3.1 — Section/Report template, RecordFilter, export

Status: Completed

Owner: Codex

Reviewer: User-approved on 2026-09-26

Depends On: M2.3

## Objective

Execute the M3.1 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `SectionLayout` (`?view=report`, resets on path change), inserted into the route tree for every wrapped area (Phase 2 §10) with `reportKey` handles.
  - `features/reports` begins: `SectionReport` plus the reachable `REPORT_DEFS` keys. Reachability is verified at phase start; unreachable keys are listed under D11.
  - `features/organization` adds the RF vocabulary, `RecordFilter` and reference hooks.
  - `shared/filter/*`.
  - `shared/form`: `Select` (searchable combobox), `MultiSelectChips`, `DateField`, `DateRangeField`, `Checkbox`, `Field`, `FieldLabel`.
  - `shared/ui/overlay`: `Dialog` parts, `DropdownMenu`.
  - `Toaster`.
  - `shared/file`: csv, download, print.
  - Chip tones.
  - Placeholders now render inside `SectionLayout`.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

- Report tab parity on every wrapped section route (explicit keys) and on placeholder routes (fallback definition).
  - RecordFilter parity for each kind: fields, quick flags, applied chips, clear all, date min/max linkage.
  - CSV export is byte-identical to the prototype's for the same data (golden file); the Excel variant's MIME type and filename match.
  - PDF and Print both call `window.print`.
  - Select: type-to-search, ↑/↓, Enter, Escape, "No matches".
  - DateField: Today, Clear, out-of-range days disabled, Arabic month and weekday labels.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): template sign-off; template sign-off (about 12 screens reuse it).

## Prototype Verification

Source: `SectionShell` 1802–1815; `REPORT_DEFS`/`SR_DEF`/`SectionReport` 1729–1801; `RF_*` 1416–1455; `RFExport` 1457–1488; `RFDate` 1489–1564; `__DateField` 1565–1576; `RecordFilter` 1577–1727; select and date patch behavior 50–96.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M2.3

Packages: None

Shared infrastructure: SectionLayout, SectionReport, RecordFilter, shared/filter, Select, MultiSelectChips, DateField, DateRangeField, Checkbox, Field, Dialog, DropdownMenu, Toaster, file csv/download/print, useSearchParamState

## Files / Areas Expected

- `src/app/layouts/section/` and the existing route tree.
- `src/features/reports/` and `src/features/organization/`.
- `src/shared/filter/`, `src/shared/form/`, `src/shared/file/`, and the approved overlay/feedback primitives.
- M3.1 unit, route, export-golden, and visual QA coverage.
- Applicable migration registers and `docs/migration/qa/M3.1/` evidence.

## PR

Not opened. All M3.1 changes and evidence are local; no commit or push was performed.

## Review Findings

- `SectionLayout` mounts `?view=report` and resets to `?view=section` on path change, wrapping every M3.1 route (Finance & Budgets/Overtime, HR/Overtime, Appraisal, Quality & Compliance/Checklist, Workflows, History/Notifications-adjacent areas) via `reportKey` route handles.
- `REPORT_DEFS` exposes the reachable keys (budgeting, overtime, appraisal, checklist, workflows, notifications, history); `joborders`, `observations`, `processes`, `purchasing`, `pettycash`, and `approve` remain excluded per D11 reachability analysis and are not migrated.
- `RecordFilter` reproduces prototype parity for fields, quick flags, applied chips, clear-all, and date min/max linkage, including the searchable `Select`, `MultiSelectChips`, `DateField`/`DateRangeField`, and `Checkbox` primitives.
- CSV export is byte-identical to the prototype's golden fixture (`scripts/golden/m3.1-overtime-csv.fixture.json`); Excel export MIME type/filename match, and PDF/Print both invoke `window.print`.
- `Dialog`, `DropdownMenu`, and `Toaster` were added as the approved shared overlay/feedback primitives; no additional packages were introduced.
- Delta intake against baseline `273abc8` found no relevant source drift in the M3.1 prototype ranges (`SectionShell`, `REPORT_DEFS`/`SR_DEF`/`SectionReport`, `RF_*`, `RFExport`, `RFDate`, `__DateField`, `RecordFilter`, select/date patch behavior) between baseline and prototype HEAD `3c391b4`.

## Deviations

No unapproved architecture deviation was introduced. Unreachable report keys (joborders, observations, processes, purchasing, pettycash, approve) were intentionally excluded per D11 and are not a deviation.

## Blockers

No implementation blocker. Remote CI/PR execution was not run because no PR/commit was requested.

## Human Approval

Approved by the user on 2026-09-26. M3.2 has not started.

## Completion Notes

### Phase and prototype intake

- Phase: M3.1 — Section/Report template, RecordFilter, export.
- Prototype ranges reviewed: `SectionShell` 1802-1815; `REPORT_DEFS`/`SR_DEF`/`SectionReport` 1729-1801; `RF_*` 1416-1455; `RFExport` 1457-1488; `RFDate` 1489-1564; `__DateField` 1565-1576; `RecordFilter` 1577-1727; select/date patch behavior 50-96.
- R6 result: no relevant source delta from baseline `273abc8` to prototype HEAD `3c391b4`; both revisions share the identical `index.html` blob for these ranges.

### Delivered scope

- `SectionLayout` (`src/app/layouts/section/`) adds the `Section`/`Report` tab switch driven by `?view=report`, resets to the default view on path change, and is wired into the route tree via `handles.ts` `reportKey`s for every wrapped area.
- `features/reports` (`SectionReport`, `report-definitions.ts`, `report.types.ts`) implements the reachable `REPORT_DEFS` keys; the fallback placeholder definition covers routes without an explicit key.
- `features/organization` adds the RecordFilter vocabulary (`filter.types.ts`), `RecordFilter`/`RecordExport` components, `useReferenceFilters`, and the reference-filter mock data.
- `shared/filter`, `shared/form` (`Select`, `MultiSelectChips`, `DateField`, `DateRangeField`, `Checkbox`, `Field`, `FieldLabel`), `shared/file` (csv/download/print), and `shared/ui/overlay` (`Dialog`, `DropdownMenu`) plus `Toaster` were added as shared infrastructure.
- Chip tones were extended in `shared/ui/chip/Chip.tsx` to support the RecordFilter applied-chip states.
- Placeholder routes render inside `SectionLayout` instead of as bare `MigrationPending` pages.

### Responsive, RTL, and visual QA

- Full evidence matrix: five states × four widths × two themes × two locales = 80 side-by-side captures.
- States: report-default, record-filter-dialog, record-filter-calendar, record-filter-applied, export-menu.
- Evidence: `docs/migration/qa/M3.1/<state>/<width>-<theme>-<locale>.png`, captured with `npm run visual:capture:m3.1` (`tests/visual/m3.1.spec.ts`) against the read-only prototype at `PROTOTYPE_DIR`.
- Spot-reviewed captures (1440/390/1040 across paper/ink and en/ar) show layout, copy, RTL mirroring, and interaction-state parity with the prototype; no open material mismatch was found.

### Quality gates

- G1: pass — `npm.cmd run typecheck`, 0 errors.
- G2: pass — `npm.cmd run lint`, 0 errors/warnings.
- G3: pass — targeted M3.1 tests: 111 passed. Full suite: 28 files, 189 tests passed.
- G4: pass — `npm.cmd run build`; Vite emitted only the pre-existing >500kB chunk advisory. `npm.cmd run check:bundle` passed, 13 files inspected.
- G7: pass — 80/80 side-by-side visual captures produced and spot-reviewed for report template, RecordFilter (dialog/calendar/applied), and export-menu states.
- G8: pass — every new export has an M3.1 consumer; unreachable report keys were not scaffolded.
- G9: pass — no package was added or changed (`Packages: None`); only shared infrastructure listed in scope was introduced.
- G10: pass — `npm.cmd run report:pending` passed with the two expected pre-existing markers (`src/app/router/MasterRoutePage.tsx`, `src/app/router/NavRoutePage.tsx`); delta/decision/deviation registers were reviewed and updated (`docs/migration/PROTOTYPE_DELTAS.md`).
- Golden parity: pass — overtime CSV export is byte-identical to the prototype golden fixture.

## User-visible behavior

1. Every wrapped section route now exposes a Section/Report tab switch (`?view=report`) that resets to Section on navigation to a different path.
2. The Report tab renders the reachable report (budgeting, overtime, appraisal, checklist, workflows, notifications, history) with report format, group-by, and period controls, a live preview, and Export/Email actions.
3. Filters open a dialog with quick-flag chips, record fields, applied-chip summary, clear-all, and a date range with a calendar picker (Today/Clear, disabled out-of-range days, localized month/weekday labels in Arabic).
4. Export opens a dropdown with Excel spreadsheet, CSV file, PDF document, and Print; CSV output matches the prototype byte-for-byte, and PDF/Print both call `window.print`.
5. Unreachable report keys (joborders, observations, processes, purchasing, pettycash, approve) are not present in the migrated app, matching their prototype unreachability.
6. RTL/Arabic and dark-mode (ink) rendering mirror the prototype across 1440/1040/760/390 widths.

**M3.1 is approved and completed. M3.2 has not started.**

