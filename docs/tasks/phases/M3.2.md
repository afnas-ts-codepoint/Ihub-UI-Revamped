# M3.2 — Appraisal

Status: Completed

Owner: Codex

Reviewer: User-approved on 2026-09-26

Depends On: M3.1 (approved 2026-09-26)

## Objective

Migrate the prototype Appraisal screen at `/appraisal`, introduce only the table infrastructure first required by this screen, run the M3.2 quality gates and reduced visual matrix, then stop for human approval.

## Prototype sources reviewed

- `AppraisalScreen`: `index.html` lines 8466-8569.
- `TabbedTable`: `index.html` lines 8070-8227.
- `STATUS_PILL`: `index.html` lines 8271-8284.
- `EmptyState`: `index.html` lines 13191-13232, as identified by the approved Phase 2 architecture for M3.2.

## Delta intake result

Prototype baseline `273abc8` was compared with current prototype HEAD `3c391b4`. `index.html` has no diff between those revisions, so every M3.2 source range is unchanged. No adopt/defer decision was required. The result is recorded in `docs/migration/PROTOTYPE_DELTAS.md`.

## Scope implemented

- Added `src/features/appraisal/` with the Appraisal page, table composition, typed prototype data, data hook, feature types, and the pure score-band rule.
- Replaced the `/appraisal` `MigrationPending` route element with `AppraisalPage` while retaining the approved `SectionLayout` and `reportKey: 'appraisal'` integration.
- Added `src/shared/table/` primitives, `TabbedTable`, and deliberately inert `TablePagination` without a table framework or page state.
- Added the reusable `EmptyState` feedback component and used it for zero-row table rendering.
- Added the Appraisal EN/AR namespace and typed i18n registration.
- Added focused Appraisal, score-band, shared-table, route-integration, and visual-QA coverage.

## Files and folders changed

- `src/features/appraisal/`: page, component, hook, mock data, domain rule, types, public API, and tests.
- `src/shared/table/`: table primitives, `TabbedTable`, `TablePagination`, public API, and tests.
- `src/shared/ui/feedback/EmptyState.tsx`.
- `src/shared/i18n/locales/{en,ar}/appraisal.json`, i18n initialization/types/parity test.
- `src/app/router/router.tsx`, `src/app/navigation/nav.config.ts`, and `src/app/layouts/section/SectionLayout.test.tsx`.
- `tests/visual/m3.2.spec.ts`, `package.json`, and `docs/migration/qa/M3.2/`.
- M3.2 task report and the delta, NOOP, and pending registers.

## Appraisal route and SectionLayout

- Migrated route: `/appraisal`.
- Section view renders `AppraisalPage` inside the existing M3.1 `SectionLayout`.
- `/appraisal?view=report` continues to render the approved Appraisal report through `reportKey: 'appraisal'`.
- No `SectionLayout` production code was changed.

## Tabs and counts

- One tab, in prototype order: `All Appraisals`.
- Literal count: `5`, derived from the exact five-row prototype dataset.
- The tab is active by default; generic active-tab switching is covered by the shared-table test.

## Table implementation and row parity

Columns and order match the prototype: Appraisal #, Employee, Department, Period, Score, Rating, Reviewer.

Rows match `AppraisalScreen` exactly:

1. `APR-2025-118`, Sara Al-Qahtani, Marketing, Q1 2025, 4.6, Exceeds, A. Al-Rashid.
2. `APR-2025-117`, Khaled Ibrahim, Operations, Q1 2025, 4.1, Exceeds, A. Al-Rashid.
3. `APR-2025-116`, Layla Haddad, Marketing, Q1 2025, 3.8, Meets, A. Al-Rashid.
4. `APR-2025-115`, Mohammed Al-Otaibi, Finance, Q1 2025, 3.4, Meets, N. Saleh.
5. `APR-2025-114`, Yousef Al-Mutairi, IT, Q1 2025, 2.9, Improve, N. Saleh.

## Status-pill mapping

Appraisal owns the prototype mapping and reuses the shared M3.1 `Chip` presentation:

- Exceeds → `ok`.
- Meets → `neutral`.
- Improve → `warn`.

Every Appraisal status is asserted in component coverage.

## Score-band implementation and tests

`domain/scoreBand.ts` is pure and independently tested:

- score `>= 4` → `ok`.
- score `>= 3` and `< 4` → `neutral`.
- score `< 3` → `bad`.

Boundary coverage passes for 2.9, 3.0, 3.8, 4.0, and 4.6. The table formats scores to one decimal place and applies the returned band.

## EmptyState

The generic reusable `EmptyState` follows the approved prototype treatment. `TabbedTable` renders it for a zero-row input, with title and description supplied by Appraisal. The migrated prototype dataset is non-empty, so this state is unit-tested but not reachable on the default `/appraisal` screen.

## TablePagination and PROTOTYPE-NOOP(D2)

The visible previous, page 1-3, and next controls match the prototype. They intentionally have no handlers, page state, query parameters, or row slicing. Tests click next and page 2 and verify page 1 remains current, the row remains visible, and navigation does not occur.

Registered in `docs/migration/PROTOTYPE_NOOPS.md` as `PROTOTYPE-NOOP(D2)`.

## i18n and RTL result

- Appraisal EN/AR resources have identical key sets and contain every user-facing Appraisal JSX label.
- Arabic uses the prototype Arabic title and the existing localized RecordFilter.
- The prototype keeps Appraisal tab, table, row, rating, and pagination-summary wording in English in Arabic mode; production preserves that behavior through the Arabic resource instead of hardcoded JSX.
- RTL mirrors tab, table, score alignment, filter layout, and pagination using logical alignment utilities. Western digits remain in use per D20.

## Responsive result

- At desktop width the complete seven-column table is visible.
- At 760px and 390px the table remains a table and scrolls horizontally, matching the prototype; no card transformation was added.
- Tabs, count, rows, pills, scores, pagination, and EmptyState retain their table presentation.

## Reduced visual-matrix evidence

`npm.cmd run visual:capture:m3.2` passed 10/10 captures: two states (`section`, `report`) across the five approved reduced variants:

- 1440 Paper EN.
- 1440 Ink EN.
- 1440 Paper AR.
- 760 Paper EN.
- 390 Paper EN.

Evidence is stored under `docs/migration/qa/M3.2/<state>/<width>-<theme>-<locale>.png`. Side-by-side review confirmed the Appraisal title, filter integration, single tab/count, exact rows, score bands, status pills, pagination, themes, RTL mirroring, horizontal mobile overflow, and Report integration. A pagination styling mismatch found during review was corrected and the matrix was regenerated. No material Appraisal difference remains open.

## Dependency diff

No dependency or lockfile change. `package.json` only adds the `visual:capture:m3.2` script. No table framework or other package was added.

## Quality gates

- G1 typecheck: pass — `npm.cmd run typecheck`, 0 errors.
- G2 lint: pass — `npm.cmd run lint`, 0 errors/warnings.
- G3 tests: pass — full suite 33 files, 204 tests; focused M3.2 suite 3 files, 10 tests.
- G4 build + bundle: pass — `npm.cmd run build`; only the pre-existing >500 kB chunk advisory. `npm.cmd run check:bundle` passed, 15 files inspected.
- G5 i18n parity: pass — Appraisal EN/AR keys are included in the active parity test.
- G6 nav↔route parity: pass — targeted navigation/SectionLayout suite 78 tests; full suite green.
- G7 fidelity evidence: pass — 10/10 reduced-matrix side-by-side captures reviewed with no open material difference.
- G8 no speculative code: pass — shared table primitives, `TabbedTable`, `TablePagination`, and `EmptyState` all have M3.2 consumers; no future table engine/API layer was added.
- G9 dependency diff: pass — no dependency or lockfile change.
- G10 registers: pass — prototype delta, NOOP, and MigrationPending registers updated; no architecture deviation was needed.

## Regression results

- Full Vitest suite: 33 files and 204 tests passed, including M1/M2/M3.1 coverage.
- M3.2 route test verifies the Appraisal screen replaces `MigrationPending` and `/appraisal?view=report` still renders its report.
- `npm.cmd run report:pending` passed with the same two shared implementation markers in `MasterRoutePage.tsx` and `NavRoutePage.tsx`; the Appraisal nav marker is removed while future-phase markers remain.
- `git diff --check` passed.

## Register updates

- `PROTOTYPE_DELTAS.md`: added the no-delta M3.2 intake result.
- `PROTOTYPE_NOOPS.md`: added inert Appraisal pagination as `PROTOTYPE-NOOP(D2)`.
- `MIGRATION_PENDING.md`: Appraisal marked removed, awaiting human approval.
- `DEVIATIONS.md`: unchanged because no new deviation was introduced.

## Deviations

None.

## Unresolved blockers

None.

## User-visible behavior

1. `/appraisal` is now migrated.
2. Users see the real Appraisal section instead of `MigrationPending`, inside the existing Section/Report layout.
3. The screen shows one active tab: `All Appraisals`, count 5.
4. The seven prototype columns and all five exact prototype rows appear.
5. Exceeds is green/ok, Meets is neutral, and Improve is amber/warn through the existing Chip styling.
6. Scores show one decimal place; 4.0+ is ok/green, 3.0-3.9 is neutral, and below 3.0 is bad/red.
7. Pagination shows previous, pages 1-3, and next, and intentionally remains inert for prototype parity (`PROTOTYPE-NOOP(D2)`).
8. Arabic switches the title and existing filter UI, mirrors the layout in RTL, preserves the prototype's English table vocabulary, and retains Western digits.
9. On smaller screens the table scrolls horizontally; it does not transform into cards.
10. No unrelated route changed. HR remains in its pre-M3.3 pending state.

## PR

Not opened. No commit or push was requested.

## Human approval

M3.2 was approved by the user on 2026-09-26.

**M3.3 has not started.**
