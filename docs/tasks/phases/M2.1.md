# M2.1 — Navigation model and route map

Status: Completed

Owner: Codex

Reviewer: User

Depends On: M1.4

## Objective

Execute the M2.1 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `app/navigation/*`:
    - typed config, keeping prototype ids in `NavNode.id`;
    - labels in the `nav` namespace, extracted verbatim;
    - a pure model with tests, plus `useNavTrail`.
  - `features/masters/catalogue/` + `features/masters/index.ts` (catalogue only).
  - `shared/config/paths.ts`.
  - Route areas implementing the Phase 2 §11 URL map:
    - screens that exist in the prototype render `MigrationPending`;
    - prototype stub leaves render `PlaceholderPage`: title, "Module landing — connect your data to see live content." and the scaffold body;
    - redirects for `/`, `/home` and first-leaf (D10);
    - Home nav children mapped per D13; `homeTab` route handles;
    - no routes for D11 items.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

- G6 parity test green.
  - Placeholder titles match the prototype in EN and AR.
  - The URL map matches Phase 2 §11 row by row.
  - Unit tests cover trail, first leaf and ancestor.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): D10, D13, D14; D10, D13, D14.

## Prototype Verification

Source: `NAV_TREE` 2460–2591; nav helpers 2592–2613; `MASTERS_CATEGORIES` 2423–2457; `MASTERS_WITH_PAGE` 2458; App route switch 22416–22528; `StubScreen` 7856–7937 and App stub text 22524–22528.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M1.4

Packages: None

Shared infrastructure: nav config/model, `paths.ts`, PlaceholderPage, masters catalogue

## Files / Areas Expected

- `src/app/navigation/`: complete typed prototype tree, pure navigation model, `useNavTrail`, and tests.
- `src/app/router/`: Phase 2 Section 11 routes, redirects, pending/placeholder selection, and G6 tests.
- `src/features/masters/catalogue/` and `src/features/masters/index.ts`: the approved 80-entry catalogue only.
- `src/shared/config/paths.ts` and the EN/AR `nav` namespace.
- M2.1 tracker, delta, and pending-marker register updates.

## PR

Not opened. No commit or push was performed; remote CI therefore remains pending until an authorized PR exists.

## Review Findings

- The production navigation tree preserves all 271 generated prototype IDs in source order: 101 non-Masters nodes and 85 nodes in each D10 Masters variant.
- Real prototype screens route to two shared `MigrationPending` source markers; screenless nav leaves route to the final prototype-faithful `PlaceholderPage`.
- `/` and `/home` redirect to `/home/overview`; both Masters roots and every category redirect to their first leaf.
- Every Phase 2 Section 11 parameter family is validated, all Home routes expose the approved `homeTab` handle, and D11 routes remain 404s.
- EN/AR nav labels are loaded from the `nav` namespace. Masters item labels intentionally retain the prototype's English fallback in Arabic.
- `PlaceholderPage` reproduces the prototype header text, six-card scaffold, leading square marker, spacing, and scaffold footer. Its Section/Report toggle remains deferred to M3.1 as planned.

## Deviations

None.

## Blockers

No implementation blocker. Remote CI remains unexecuted until a future authorized commit/PR.

## Human Approval

Approved by user on 2026-09-25: “i verified confirms M2.1”.

## Completion Notes

- Delivered: the full typed prototype navigation tree, pure path/trail/first-leaf/ancestor model, `useNavTrail`, the complete Phase 2 Section 11 route map, D10 redirects, D13 Home mappings/handles, D14 placeholders, and explicit D11 exclusions.
- Masters: 27 Admin + 22 General + 16 HR + 15 Operation = 80 entries in each route variant; 5 real screens are pending and 75 screenless leaves are placeholders.
- Shared infrastructure: navigation config/model/hook, `paths.ts`, `PlaceholderPage`, route dispatch helpers, and the Masters catalogue. Packages added: none.
- R6: no relevant prototype delta from baseline `273abc8` to current prototype HEAD `3c391b4` across all M2.1 source ranges.
- G1: pass, `npm run typecheck` (0 errors).
- G2: pass, `npm run lint` (0 errors).
- G3: pass, `npm run test` (18 files, 138 tests).
- G4: pass, `npm run build` and `npm run check:bundle` (production build complete; 9 text bundle files inspected).
- G5: pass within the full test run; EN/AR resource keys remain in parity.
- G6: pass; 271 nav nodes resolve, configured behavior renders in EN and AR, unknown/D11 paths return 404, all URL-map families and `homeTab` handles are covered, and D10 redirects are tested.
- G7: not active until M2.2. M2.1 automated DOM checks cover the required placeholder text and six-card scaffold; the visual capture harness begins in M2.2.
- G8: pass; every addition is required by M2.1 and has a production or acceptance-test consumer. No speculative feature folder or API was added.
- G9: pass; `package.json` and `package-lock.json` are unchanged.
- G10: pass; the full R6 result and every active route family sharing the two `MigrationPending` source markers are registered. No NOOP or deviation applies.
- Formatting: `npx prettier --check "src/**/*.{ts,tsx,json}"` passes; `git diff --check` reports no whitespace errors.
- QA evidence: source ranges were checked against the unchanged prototype; behavior tests cover redirects, labels, route classification, validation, placeholders, unknown paths, and both router modes. No material difference remains in M2.1 scope.
- Open questions: none. M2.2 remains pending and is not started automatically.

