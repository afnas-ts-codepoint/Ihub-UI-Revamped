# M1.2 — Design tokens, fonts, base CSS, Tailwind

Status: Review

Owner: Codex

Reviewer: Not assigned

Depends On: M1.1

## Objective

Execute the M1.2 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `styles/tokens.css`: prototype names and values verbatim, including `--accent-dim: #93358D24`; Ink under `:root[data-theme="ink"]`.
  - `styles/fonts.css` + `assets/fonts/`: Zarid Sans, BCN Arabic, GE SS, Cormorant italic and `NumCalibri`, self-hosted, no base64.
  - `styles/base.css`: body type, RTL font stack and 1.58 line-height, focus ring, scrollbars, emphasis motif, `.num` tabular numerals, logo swap, sRGB chip-tone `@utility`.
  - `styles/index.css`: `@theme inline` aliases (Phase 2 §22), invariant scales, desktop-first breakpoints (1041 / 761 / 481), default palette and radius reset.
  - `shared/lib/cn.ts` with `extendTailwindMerge`.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

- Token parity test passes 100%. It compares against a fixture of prototype values with line references.
  - Setting `data-theme` on `<html>` switches every surface.
  - `dir="rtl"` switches the font stack and line-height.
  - A default-palette class compiles to nothing.
  - `cn` merge tests pass, including `text-fg-2` vs `text-sm`.
  - No base64 fonts in the bundle.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): D18, Q5; D18 (missing fonts), Q5 (licence).

## Prototype Verification

Source: `installGlobalStyles` 134–612 (fonts 139–201, Paper tokens 203–261, Ink 263–285, base rules 287–612); head styles 6–41; runtime accent 22380–22388.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M1.1

Packages: tailwindcss, @tailwindcss/vite, prettier-plugin-tailwindcss, clsx, tailwind-merge

Shared infrastructure: tokens.css, fonts.css, base.css, `@theme`, `cn`

## Files / Areas Expected

- `src/styles/{index,tokens,fonts,base}.css` and their M1.2 tests/fixture.
- `src/assets/fonts/` containing the ten approved prototype font files.
- `src/shared/lib/cn.ts` and its merge tests.
- Tailwind Vite/Prettier configuration, the application stylesheet import, and the bundle font check.
- M1.2 tracker, delta, and decision-register updates.

## PR

Not opened. No commit or push was performed; remote CI therefore remains pending until an authorized PR exists.

## Review Findings

- Token parity covers 59 approved Paper, Ink, radius, and font variables with prototype source-line references.
- All 17 Ink overrides switch from `<html data-theme="ink">`; `<html dir="rtl">` selects the Arabic stack and line-height `1.58`.
- Tailwind's scanner is constrained to production `src` inputs and excludes tests/fixtures, preventing example lint strings from entering the bundle.
- The default red palette class compiles to nothing while semantic colors compile; the production CSS contains no `bg-red-500`, `pl-4`, or arbitrary magenta utility.
- The ten copied font files match the prototype SHA-256 hashes. The two Cormorant files also match the prototype's former base64 payloads.
- `NumCalibri` preserves the prototype's local Calibri/Carlito/Segoe UI sources because no NumCalibri asset exists.
- Feature-owned responsive and component CSS from the source ranges was not pulled forward; later owning phases remain responsible for it.
- `npm ci --dry-run` validates the lockfile. An in-place `npm ci` could not remove Tailwind/Rolldown native binaries while the user's Vite dev server was running; that process was left untouched and `npm install` restored the local dependency tree with 0 vulnerabilities.

## Deviations

None. The approved architecture moves theme and direction attributes from `<body>` to `<html>`. D18 drops only the missing Myriad Pro and GE SS Light Italic faces; no substitute assets were added.

## Blockers

No implementation blocker. Remote CI remains unexecuted until a future authorized commit/PR. A fully destructive in-place `npm ci` must wait until the currently running Vite dev server releases its native binaries; the lockfile dry run passes.

## Human Approval

Required

## Completion Notes

- Delivered: Paper/Ink tokens, exact runtime accent, self-hosted available fonts, base/RTL rules, sRGB chip utilities, Tailwind v4 theme scales and semantic aliases, and configured `cn()`.
- Packages added: `tailwindcss@4.3.3`, `@tailwindcss/vite@4.3.3`, `prettier-plugin-tailwindcss@0.8.1`, `clsx@2.1.1`, `tailwind-merge@3.7.0`.
- R6: no relevant prototype source delta from baseline `273abc8`.
- G1: pass, `npm run typecheck` (0 errors).
- G2: pass, `npm run lint` (0 errors).
- G3: pass, `npm run test` (3 files, 10 tests).
- G4: pass, `npm run build` and `npm run check:bundle` (fonts emitted as files; 3 text bundle files inspected).
- G5-G7: not active at M1.2.
- G8: pass; only the phase-card stylesheet infrastructure and tested `cn` export were introduced.
- G9: pass; exactly the five authorized M1.2 packages were added.
- G10: pass; delta and decision registers were updated. No deviation, NOOP, or MigrationPending entry was applicable.
- Acceptance: 100% fixture parity; Ink and RTL switching pass; default palette suppression passes; `cn` merge cases pass; no base64 font data is present.
- Differences: none beyond the approved `<html>` attribute placement and D18 omissions.
- Open questions: none. Human approval is requested before M1.3.

