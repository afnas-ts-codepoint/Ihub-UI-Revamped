# Prototype No-Ops

Track visible prototype controls that intentionally remain non-functional.

| ID                 | Feature   | Control                  | Prototype reference          | Current behavior                                                                            | Migration decision                                                                                    | Phase | Status |
| ------------------ | --------- | ------------------------ | ---------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ----- | ------ |
| PROTOTYPE-NOOP(D2) | Workflows | New Rule | `index.html` line 21431 | The visible primary action has no click handler and creates no rule. | Preserve the visible inert control for prototype parity; workflow authoring is not introduced in M3.7. | M3.7 | Active |
| PROTOTYPE-NOOP(D2) | Workflows | Rule and step Edit | `index.html` lines 21444, 21481 | The visible edit-icon controls do not open an editor or change workflow data; rule Edit only stops row expansion. | Preserve both visible inert edit controls for prototype parity. | M3.7 | Active |
| PROTOTYPE-NOOP(D2) | Workflows | Discard | `index.html` line 21408 | The visible control has no click handler and changes no state. | Preserve the visible inert control for prototype parity. | M3.7 | Active |
| PROTOTYPE-NOOP(D2) | Workflows | Save Draft | `index.html` line 21409 | The visible control has no click handler and persists nothing. | Preserve the visible inert control for prototype parity. | M3.7 | Active |
| PROTOTYPE-NOOP(D2) | Workflows | Publish | `index.html` line 21410 | The visible control has no click handler and does not change the published version. | Preserve the visible inert control and literal `v3.2` badge for prototype parity. | M3.7 | Active |
| PROTOTYPE-NOOP(D2) | Appraisal | `TabbedTable` pagination | `index.html` lines 8181-8226 | Previous, pages 1-3, and next render but do not change the five displayed rows or navigate. | Preserve the visible inert controls for prototype parity; functional table pagination begins in M4.1. | M3.2  | Active |
| PROTOTYPE-NOOP(D2) | HR (Overtime) | Overtime tab literal counts | `index.html` lines 8289-8317 | The seven tab badges (14, 6, 3, 2, 4, 1, 92) are hardcoded and do not match the six-row mock dataset; clicking a tab does not filter or recompute the badge or the displayed rows. | Preserve the literal `displayCount` values for prototype parity; not a bug, no dynamic/recomputed count is introduced. | M3.3  | Active |
| PROTOTYPE-NOOP(D2) | HR (Overtime) | `TabbedTable` pagination | `index.html` lines 8181-8226 | Previous, pages 1-3, and next render but do not change the six displayed rows or navigate. | Preserve the visible inert controls for prototype parity; functional table pagination begins in M4.1. | M3.3  | Active |
| PROTOTYPE-NOOP(D2) | Notifications | Unread tab literal count | `index.html` lines 12098-12107 | The `Unread` tab badge is hardcoded to `3`; the five displayed rows carry no per-row unread flag, so clicking the tab does not filter or recompute the badge or the displayed rows. | Preserve the literal `Unread` count for prototype parity; not a bug, no dynamic/recomputed count is introduced. | M3.4  | Active |
| PROTOTYPE-NOOP(D2) | Notifications | `TabbedTable` pagination | `index.html` lines 8181-8226 | Previous, pages 1-3, and next render but do not change the five displayed rows or navigate. | Preserve the visible inert controls for prototype parity; functional table pagination begins in M4.1. | M3.4  | Active |
| PROTOTYPE-NOOP(D2) | Quality Checklist | Checklist tab literal counts | `index.html` lines 8647-8659 | The two badges (`4`, `23`) are hardcoded; clicking either tab changes only the active style and continues to show the same four rows. | Preserve both literal `displayCount` values and the shared-row tab behavior; do not derive counts or filter rows. | M3.5 | Active |
| PROTOTYPE-NOOP(D2) | Quality Checklist | `TabbedTable` pagination | `index.html` lines 8181-8226 | Previous, pages 1-3, and next render but do not change the four displayed rows or navigate. | Preserve the visible inert controls for prototype parity; functional table pagination begins in M4.1. | M3.5 | Active |
| PROTOTYPE-NOOP(D2) | History | `TabbedTable` pagination | `index.html` lines 8181-8226, 9774-9779 | Previous, pages 1-3, and next render but do not change the ten displayed rows or navigate on any History scope. | Preserve the visible inert controls for prototype parity; functional table pagination begins in M4.1. | M3.6 | Active |
