# M8.3 — Create Task (canonical)

Status: Pending

Owner: Not assigned

Reviewer: Not assigned

Depends On: M8.1, M6.5

## Objective

Execute the M8.3 Phase 3 task exactly as defined, then stop for human approval.

## Scope

`/home/work-centre/create-task` with every section:
  - Location profiles in a drawer: a separate form instance; add another, edit and delete; the "Please fill in required fields: …" summary.
  - QR scan dialog: random registry pick via the seedable `random`, then the `applyLocProfile` fill.
  - Chips, team auto-suggest from the task-type roadmap, and a checklist with assignee pickers.
  - Dependency links with relationship types.
  - Attachments: drag-drop, preview, download, remove, and the demo files.
  - Requester auto-fill.
  - Submit writes to `tasks.store`. Post-submit behavior is verified at phase start. There is no blocking validation (D2).

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

parity per section; the QR fill is identical with `Math.random` stubbed in both apps; the new task appears in the list.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): delta intake

## Prototype Verification

Source: `CreateTaskPanelDesignChange` 20196–20862; `ctpSampleAttachments` 19521–19546.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M8.1, M6.5

Packages: None

Shared infrastructure: FileDropZone, AttachmentPreviewDialog, useObjectUrl, seedable random

Task-card additions: `FileDropZone`, `AttachmentPreviewDialog`, `useObjectUrl`, seedable `random`.

## Files / Areas Expected

TBD at task start from the approved scope; do not create speculative files or empty folders.

## PR

TBD

## Review Findings

TBD

## Deviations

TBD

## Blockers

TBD

## Human Approval

Required

## Completion Notes

TBD

