# M3.6 — History

Status: Completed

Owner: Codex

Reviewer: User-approved on 2026-09-26

Depends On: M3.2

## Objective

Execute the M3.6 Phase 3 task exactly as defined, then stop for human approval.

## Scope

`features/history` at `/history/*`, reading module and category from the path. Row generation (scope → prefix, kind, 10 rows) is ported as a pure function.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

for every History nav leaf, refs, actions, users, dates, tones and the RecordFilter kind all equal the prototype (checked through the UI).

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, request human approval, and stop.
Specific gate(s): None beyond the applicable standard gates and human approval.

## Prototype Verification

Source: `HistoryScreen` 9729–9780.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M3.2

Packages: None

Shared infrastructure: None

## Files / Areas Expected

- `src/features/history/`: typed scope mapping, pure row generator, page, table, hook, types, public API, and tests.
- `src/shared/i18n/locales/{en,ar}/history.json` and active i18n registration/parity coverage.
- Existing router/navigation integration only; no new shared UI infrastructure.
- `tests/visual/m3.6.spec.ts`, `package.json` script, and `docs/migration/qa/M3.6/` evidence.
- M3.6 task report plus delta, NOOP, and pending-register updates.

## PR

Not opened. No commit or push was requested.

## Review Findings

- All 28 History navigation-node paths now resolve to one route-derived `HistoryPage`; all 23 terminal History leaves were compared directly with the running prototype.
- `generateHistoryRows` ports the prototype's exact deterministic scope-label algorithm, including the locale-dependent label length, English prefix extraction/fallback, action selection, tone, user rotation, fixed date order, and 10-row count.
- Every History path retains its exact prototype `RecordFilter` kind. In particular, `/history/finance-budgets/payment-settlement` and `/history/finance-budgets/petty-cash` correctly fall back to `history`, while the Finance & Budgets group/new/additional/transfer paths use `budget`, and `/history/purchasing` uses `request`.
- Existing `RecordFilter`, `TabbedTable`, `TablePagination`, `EmptyState`, `Chip`, and `SectionLayout` implementations are reused without creating a History-specific duplicate or a new table system.
- Invalid History paths use the existing 404 route. History-specific pending markers were removed without changing future-phase markers.

## Deviations

None. No unapproved architecture or prototype-behavior deviation was introduced.

## Blockers

None. Remote CI/PR execution was not run because no PR or commit was requested.

## Human Approval

Approved by the user on 2026-09-26. M3.7 has not started.

## Completion Notes

### Phase and prototype intake

- Phase: M3.6 — History.
- Prototype source reviewed: `HistoryScreen` lines 9729-9780 plus navigation/index lookup, `RecordFilter`, `TabbedTable`, `STATUS_PILL`, `PageHeader`, `SectionShell`, and route-switch dependencies.
- Delta intake: prototype HEAD `3c391b4`; `index.html` has no diff from approved baseline `273abc8`. No adopt/defer decision was required.
- Plan/prototype consistency: confirmed. History is handled under `history/*`; module/category labels come from route scope; row generation is deterministic; every scope produces 10 rows; the filter kind is computed from the category/module fallback plus localized scope; and row values vary with the localized scope label exactly as currently implemented.

### Route/scope mapping

The typed mapping covers all 28 History navigation nodes:

- Root/group nodes: `/history`, `/history/work-centre`, `/history/finance-budgets`, `/history/hr`, and `/history/quality-compliance`.
- Work Centre leaves: `tasks`, `enquiry`, `observations`, `incidents`, `checklists`, `price-change`, and `promotions`.
- Finance & Budgets leaves: `new-budget`, `additional-budget`, `transfer-fund`, `payment-settlement`, and `petty-cash`.
- HR leaves: `overtime`, `investigations`, `violations`, `loan`, `end-of-probation`, and `exit-interview`.
- Other leaves: `appraisal`, Quality & Compliance `observations`, Quality & Compliance `quality-assurance-checklists`, `purchasing`, and `sop-checklist`.

The router enumerates those valid paths to the shared `HistoryPage` and sends unrecognized History paths to the approved `NotFoundPage`; no route-state persistence or duplicate routing state was added.

### Pure row generation and parity

- `generateHistoryRows(scopeLabel)` is a pure function under `features/history/domain`.
- It reproduces the prototype prefix rule, `2026000 - index * 7` references, eight-action rotation, tone mapping, seven-user rotation, fixed ten-date order, and exact row ordering.
- Exhaustive domain coverage verifies all 27 distinct EN/AR scope labels used by the 28 routes (Observations is shared by two leaves): 10 rows, every ref, action, user, date, tone, and position in both locales.
- English prefixes are scope-derived (`TAS`, `ENQ`, `OBS`, `INC`, `FIN`, `NEW`, `ADD`, `TRA`, `PAY`, `PET`, `OVE`, `INV`, `VIO`, `LOA`, `END`, `EXI`, `APP`, `QUA`, `PUR`, `SOP`, and the other exact three-letter results); Arabic labels preserve the prototype's `REC` fallback.
- Actions are Created, Submitted, Approved, Verified, Returned, Rejected, Edited, and Closed with neutral/warn/ok/bad tones exactly matching `STATUS_PILL`; users and dates are identical to the source.

### Per-leaf RecordFilter kinds

- Work Centre: Tasks → `task`; Enquiry → `enquiry`; Incidents → `incident`; Observations, Checklists, Price Change, Promotions → `history`.
- Finance & Budgets: New Budget, Additional Budget, Transfer Fund → `budget`; Payment Settlement and Petty Cash → `history`. The Finance & Budgets group itself also uses `budget`.
- HR: Overtime, Investigations, Violations, Loan, End of Probation, Exit Interview → `history`.
- Appraisal → `history`.
- Quality & Compliance: Observations and Quality Assurance Checklists → `history`.
- Purchasing → `request`.
- SOP Checklist → `history`.
- Root and non-Finance group screens use `history`.

All 23 terminal leaves were checked through Playwright against the running prototype; the comparison includes the search placeholder (and therefore filter kind), heading, complete 10-row cell matrix, order, and tone sequence.

### Shared UI, EN/AR, RTL, responsive, and Section/Report

- Reused M3.1 `RecordFilter` and `SectionLayout`, plus the M3.2-M3.5 `TabbedTable`, inert `TablePagination`, `EmptyState`, and `Chip` primitives. No shared export was added.
- Added one History i18n namespace with EN/AR key parity. Scope titles, tabs, columns, actions, notes, empty state, and pagination strings are localized; users/dates remain literal prototype data; Western digits remain per D20.
- Arabic preserves the prototype's locale-dependent `REC` refs and action/user rotation, mirrors shell/filter/table flow in RTL, and keeps the table horizontally scrollable.
- At 760px and 390px, filters wrap and the table scrolls horizontally; no unapproved card/mobile transformation was added.
- `?view=report` continues to render the existing History report for every History route. Navigating to a different History path resets Section/Report state through the unchanged M3.1 layout behavior.

### Visual evidence

- Leaf-by-leaf behavior parity: 23/23 terminal History leaves passed direct prototype-vs-application comparisons.
- Reduced visual matrix: 25 side-by-side captures under `docs/migration/qa/M3.6/`.
- States: Tasks, Payment Settlement, Purchasing, Tasks filter dialog, and History Report.
- Variants per state: 1440 Paper EN, 1440 Ink EN, 1440 Paper AR, 760 Paper EN, and 390 Paper EN.
- `npm.cmd run visual:capture:m3.6`: 48/48 checks passed (23 behavior comparisons + 25 captures). After spot review identified an asynchronous Report screenshot race in the harness, an explicit report-heading wait was added and the five Report captures were regenerated 5/5.
- Spot review covered desktop table density and tones, Arabic/RTL, mobile overflow/wrapping, filter-dialog layout, and Report mode. No open material History difference remains.

### Dependency diff

- No dependency or lockfile change.
- `package.json` adds only `visual:capture:m3.6`; no Axios, TanStack Query/Table, React Hook Form, Yup, charts, or PDF package was added.

### Quality gates

- G1 typecheck: pass — `npm.cmd run typecheck`, 0 errors.
- G2 lint: pass — `npm.cmd run lint`, 0 errors/warnings.
- G3 tests: pass — full suite 44 files, 375 tests. Focused History/i18n suite: 4 files, 134 tests; route/SectionLayout suite: 3 files, 84 tests; focused History route-transition suite: 1 file, 2 tests.
- G4 build/bundle: pass — production build completed; only the pre-existing >500 kB advisory. `check:bundle` passed with 23 files inspected.
- G5 i18n parity: pass — History EN/AR resources are included in the active parity test.
- G6 nav↔route parity: pass — all 28 History nav nodes render the real screen, valid routes resolve, and invalid History paths return 404.
- G7 fidelity evidence: pass — 23/23 direct leaf comparisons and 25/25 reduced-matrix captures, with no open material difference.
- G8 no speculative code: pass — all new feature exports and route definitions have current M3.6 consumers; no API layer, state store, or alternate table/filter system was introduced.
- G9 dependency diff: pass — no dependency or lockfile change.
- G10 registers: pass — delta, NOOP, MigrationPending, task status, and phase report updated; `report:pending` retains exactly the two expected shared implementation markers in `MasterRoutePage.tsx` and `NavRoutePage.tsx`.

### Regression results

- Full Vitest suite: 44 files and 375 tests passed, including the approved M1/M2/M3.1-M3.5 suites.
- Build and bundle inspection passed.
- `report:pending` passed with the two expected future-phase shared markers.
- `git diff --check` passed.
- No unrelated route changed.

### Register updates

- `PROTOTYPE_DELTAS.md`: recorded the no-delta M3.6 intake and consistency result.
- `PROTOTYPE_NOOPS.md`: recorded inert History `TabbedTable` pagination as `PROTOTYPE-NOOP(D2)`.
- `MIGRATION_PENDING.md`: removed the History family marker while leaving future phases intact.
- `DECISIONS.md` and `DEVIATIONS.md`: unchanged; no new decision or deviation was required.

## User-visible behavior

1. `/history` and every Work Centre, Finance & Budgets, HR, Appraisal, Quality & Compliance, Purchasing, and SOP Checklist History route listed above now render the real History screen instead of migration-pending content.
2. The current path determines the module/category label; that localized label determines the reference prefix, action sequence, user rotation, note scope, and tone sequence exactly as in the prototype.
3. Every scope shows 10 ordered rows with the prototype refs, Created/Submitted/Approved/Verified/Returned/Rejected/Edited/Closed actions, A. Al-Rashid through CEO Office user rotation, dates from 23 Jul 2026 through 05 Jul 2026, and neutral/warn/ok/bad status tones.
4. RecordFilter kinds are: Tasks `task`; Enquiry `enquiry`; Incidents `incident`; Finance group/New/Additional/Transfer `budget`; Purchasing `request`; all other terminal History scopes `history`.
5. Section mode shows the History table; Report mode continues to use the approved M3.1 History report through `?view=report`, and route transitions reset the view as before.
6. English and Arabic are supported with key parity; Arabic mirrors the layout in RTL, retains Western digits, and preserves the prototype's Arabic-scope row behavior.
7. Desktop/tablet/mobile retain the table presentation; filters wrap and tables scroll horizontally at narrow widths.
8. No unrelated route changed.

**M3.7 has not started.**

**M3.6 is approved and completed.**

