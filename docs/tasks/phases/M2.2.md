# M2.2 — Shell chrome and visual-QA harness

Status: Review

Owner: Codex

Reviewer: Not assigned

Depends On: M2.1

## Objective

Execute the M2.2 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `AppShell`.
  - `TopNav`: flat buttons and active state; burger at ≤1040px; compact bar at ≤480px.
  - `MegaMenu`: click to open; 1180px panel centered and clamped; 186px category rail; 3-column items; opens on the current category; overlay click closes.
  - `MobileNavDrawer` + `NavTreeList`.
  - `SecondaryNav`: L2/L3/L4; hidden on `/home` and `/masters`; `/masters-list` uses L2 for both levels.
  - `Logo`, colour or white per theme.
  - Icon registry: Lucide at 1.6 stroke plus custom glyphs, documented in `docs/migration/ICON_MAP.md`.
  - `UnderlineTabs`, `SegmentedControl`, `LinkTabs`, `Chip` (badge).
  - `AppProviders` with Radix `DirectionProvider`.
  - QA harness: `prototype:serve`; Playwright drivers for route, theme, Arabic via Tweaks `postMessage` and a fixed clock; a side-by-side capture script.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

- Full matrix for these states: default top nav; each top-level section active; mega menu open on every category; mobile drawer open with the tree expanded; SecondaryNav L2, L3 and L4 examples; Masters (List) strips.
  - Escape closes the mega menu and the drawer, and focus returns to the trigger.
  - RTL mirroring is correct.
  - Vaul confirmed on React 19; if not, a fallback is recorded (risk R8).

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): shell sign-off; shell sign-off and icon-map approval.

## Prototype Verification

Source: Shell top-nav branch 5180–5192; `TopNav` 3023–3121; `MegaMenuNavItem` 2897–3022; `NavTree` 2661–2709; `SecondaryNav` 5093–5171; `L2TabStrip` 5021–5092; `Logo` 2615–2660; icons 614–1097; responsive rules 489–540.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M2.1

Packages: radix-ui, vaul, lucide-react, @playwright/test, a static file server

Shared infrastructure: AppShell, TopNav, MegaMenu, MobileNavDrawer, SecondaryNav, UnderlineTabs/SegmentedControl/LinkTabs, icon registry, Chip, Drawer parts, DirectionProvider, Playwright harness

## Files / Areas Expected

- `src/app/layouts/app-shell/`: `AppShell`, `TopNav`, `MegaMenu`, `MobileNavDrawer`, `NavTreeList`, `SecondaryNav`, `Logo`, and behavior tests.
- `src/app/providers/AppProviders.tsx`: locale-derived Radix direction provider.
- `src/shared/ui/`: tab controls, icon registry, generic Chip, and Vaul Drawer parts.
- `src/assets/images/`: file-backed Paper/Ink logos and wordmarks.
- `tests/visual/`, `playwright.config.ts`, and `scripts/prototype-serve.mjs`: deterministic side-by-side QA harness.
- `docs/migration/ICON_MAP.md`, `docs/migration/PROTOTYPE_DELTAS.md`, and `docs/migration/qa/M2.2/`: review records and evidence.

## PR

Not opened. The final review fixes and regenerated evidence are local; no commit or push was performed in this continuation, so remote CI remains pending.

## Review Findings

- The global unlayered `button` reset was overriding shell active backgrounds, colors, and weights. Moving the reset into Tailwind's base layer fixes every affected shell control at the shared source.
- The mega menu now preserves the prototype's 186px category rail and three-column item grid at all approved widths, including the intentionally compressed 390px state.
- Drawer capture waits for the Vaul transition to settle, so evidence no longer records a partially translated panel.
- Mobile tree typography, indentation, bullets, and the logical active-edge marker now match the prototype in LTR and RTL.
- Underline-tab scroll arrows now point in the correct logical direction in both LTR and RTL.
- The only depth-four branch in the approved M2.1 tree is Home > Analytics & Reports > group > report. Both the prototype and architecture hide `SecondaryNav` for Home, so no reachable screen legitimately renders L4. `LinkTabs` L4 behavior and routing are covered by `SecondaryNav.test.tsx`; no production-only QA route was invented. Reviewer acceptance of this reachability interpretation is requested.

## Deviations

None. The L4 reachability note above follows the approved Home hide rule and is not an architecture deviation.

## Blockers

No implementation blocker. Remote CI and human shell/icon-map sign-off remain pending.

## Human Approval

Required. Please review the shell evidence and `docs/migration/ICON_MAP.md`; only human approval may move M2.2 to Completed.

## Completion Notes

### Phase and prototype intake

- Phase: M2.2 — Shell chrome and visual-QA harness.
- Prototype ranges reviewed: responsive rules 489–540; icons 614–1097; `Logo` 2615–2660; `NavTree` 2661–2709; `MegaMenuNavItem` 2897–3022; `TopNav` 3023–3121; `L2TabStrip` 5021–5092; `SecondaryNav` 5093–5171; shell branch 5180–5192.
- R6 result: no relevant source delta from baseline `273abc8` to prototype HEAD `3c391b4`; the existing `PROTOTYPE_DELTAS.md` entry remains current.

### Delivered shell scope

- `AppShell`: sticky shell chrome, 1440px content cap, 28/28/60 content padding, and routed `<Outlet />`.
- `TopNav`: route-aware desktop buttons, exact `<=1040px` drawer breakpoint, compact `<=480px` wordmark, shared nav model, and RTL order.
- `MegaMenu`: click open, centered/clamped 1180px panel, 186px category rail, three-column items, current-category selection, all 80 catalogue entries, scrim/Escape close, focus restoration, EN/AR, and RTL.
- `MobileNavDrawer`: React 19-compatible Vaul drawer on logical inline-start, shared recursive nav tree, route-active expansion, scrim/Escape close, focus restoration, and RTL mirroring.
- `SecondaryNav`: route-aware L2 underline and L3 segmented treatments, L4 `LinkTabs`, Home/Masters hide rules, Masters List dual-underline treatment, logical overflow controls, and RTL-safe arrows.
- `Logo`: file-backed colour/white assets with Paper/Ink swapping and compact mobile wordmark.
- Shared infrastructure: `UnderlineTabs`, `SegmentedControl`, `LinkTabs`, `Chip`, Lucide icon registry at stroke 1.6, Vaul Drawer parts, and Radix `Direction.Provider` derived from the M1.3 locale preference.

### Packages and compatibility

- Added only approved M2.2 packages: `radix-ui@1.6.7`, `vaul@1.1.2`, `lucide-react@1.48.0`, `@playwright/test@1.63.0`, and `http-server@14.1.1`.
- Vaul compatibility: passed with `react@19.3.0` / `react-dom@19.3.0`; component tests verify open, Escape, scrim close, logical RTL side, and trigger focus restoration.
- Icon map: 14 shell mappings are documented. All are Lucide mappings; no custom SVG was needed. Small path-shape differences are listed for human approval.

### QA and accessibility

- Full matrix: 16 shell states × 4 widths × 2 themes × 2 locales = 256 side-by-side captures. The final Playwright run passed 257 tests (256 captures plus the active-style browser regression) in 13.1 minutes; all 16 drawer cells were rerun after the final tree-fidelity adjustment and passed.
- Evidence: `docs/migration/qa/M2.2/<state>/<width>-<theme>-<locale>.png`.
- States: default top nav; Home, Finance, HR, Appraisal, Quality, History, and Workflows active; four Masters categories open; expanded mobile drawer; SecondaryNav L2; SecondaryNav L3; Masters List strips. L4 is component-tested because the only depth-four route is under Home, where both applications intentionally hide SecondaryNav.
- Keyboard/focus: mega menu and drawer close on Escape and scrim; tests confirm focus returns to each trigger and the drawer exposes a reasonable Radix-managed focus order.
- RTL verification: navigation order/alignment, drawer logical side, mega layout, tab alignment/arrows, directional chevrons, text alignment, active marker, overflow, and compact header were exercised across all Arabic matrix cells.
- Minor differences: anti-aliasing/font rendering and the documented Lucide path-shape differences only. No open material shell difference was found in the reviewed evidence.

### Quality gates

- G1: pass — `npm run typecheck`, 0 errors.
- G2: pass — `npm run lint`, 0 errors/warnings.
- G3: pass — `npm run test`, 20 files and 147 tests.
- G4: pass — `npm run build`; `npm run check:bundle` inspected 9 text bundle files and found no prototype tooling/base64-font regression. Vite reports its existing >500kB chunk advisory; it does not fail the approved bundle gate.
- G5: pass within the full test run — EN/AR resource keys remain in parity.
- G6: pass within the full test run — navigation/route parity, redirects, placeholders, pending behavior, and 404 handling remain green.
- G7: evidence complete — 256 regenerated side-by-side captures plus behavior checks; no open material M2.2 difference. L4 reachability is explicitly documented for reviewer sign-off.
- G8: pass — every M2.2 shared export has a shell or acceptance-test consumer; no M2.3 infrastructure or speculative feature code was added.
- G9: pass — dependency diff contains only the five packages authorized by the M2.2 card, at the exact versions above.
- G10: pass — delta, pending, NOOP, deviation, icon-map, and task registers were reviewed. No M2.2 NOOP, architecture deviation, or new `MigrationPending` marker applies. `npm run report:pending` reports the two expected shared M2.1 source markers for future feature phases.

### User-visible behavior

- The application now shows the sticky branded shell and route-aware navigation in Paper/Ink and EN/AR.
- Desktop widths above 1040px show flat top-level navigation and the click-driven Masters mega menu.
- At 1040px and below, the top-level strip becomes a logical-side mobile drawer; at 480px and below, branding compacts to the iHub wordmark.
- The Masters menu selects the current category, keeps all 80 approved entries, closes by scrim or Escape, and restores focus.
- Secondary navigation uses L2 underline tabs, L3 segmented controls, L4 quiet links where reachable, and dual underline strips for Masters List.
- RTL mirrors drawer placement, directional controls, ordering, alignment, and active markers. Ink uses the white/alternate logos.
- Sidebar layout, breadcrumbs, ContextPicker, Tweaks, FeedbackWidget, and all other excluded prototype UI remain excluded. Top-bar search, theme control UI, notifications, avatar/current-user logic, and the end-user language switch remain intentionally absent for M2.3.
- Remaining visible content differences belong to later feature phases and are represented by the already-approved M2.1 pending/placeholder behavior, not shell deviations.

**M2.3 has not started.**
