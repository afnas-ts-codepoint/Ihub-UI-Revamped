# M7.5 — Incident workspace

Status: Pending

Owner: Not assigned

Reviewer: Not assigned

Depends On: M7.1

## Objective

Execute the M7.5 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `features/incidents` at `/home/work-centre/incidents/*` and `/home/incidents/reports`.
  - Report form, list (RecordFilter `incident`) and detail view.
  - Action menu: Track, Compensate, Request investigation, Write feedback, Callback request, Close case. **Raise a task** stays `MigrationPending` until M9.2.
  - `convertToTask` as a domain function.
  - The History & updates rail.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

parity; `convertToTask` tests cover the ref format, status and log entry; Track adds the item to the tracking store.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): None beyond the applicable standard gates and human approval.

## Prototype Verification

Source: `IncidentWorkspace` 8665–8999; `RecordHUD` 1392–1411; `HUD_HISTORY` 1377–1391.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M7.1

Packages: None

Shared infrastructure: Timeline, TimelineItem, HistoryRail, useRecordHistory, tracking store

Task-card additions: `Timeline`, `TimelineItem`, `HistoryRail`; organization `useRecordHistory`; `store/tracking.store.ts`.

## Files / Areas Expected

TBD at task start from the approved scope; do not create speculative files or empty folders.

## PR

TBD

## Review Findings

TBD

## Deviations

TBD

## Blockers

TBD

## Human Approval

Required

## Completion Notes

TBD

