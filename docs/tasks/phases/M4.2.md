# M4.2 — Master record forms

Status: Pending

Owner: Not assigned

Reviewer: Not assigned

Depends On: M4.1, M3.8

## Objective

Execute the M4.2 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- View and edit dialogs; four add dialogs by definition plus a generic multi-row add.
  - Required-field rules as Yup schemas and `domain/requiredFields`.
  - Zone options narrowed by location.
  - Task Mapping: chips, severity swatches, priority radio.
  - The note "Separate Sub area will be created for each area".

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

- Golden parity of `masterFormMissing` for every mode.
  - Submit-disabled behavior matches.
  - Multi-value flattening on edit matches.
  - Add still closes without persisting, as in the prototype (registered under D2).

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): delta intake

## Prototype Verification

Source: `MasterRecordForm` 4306–4449 (**volatile**); view and edit modals 4450–4491; add modals 3751–4240; `masterFormMissing` 4281–4305; `MASTER_ADD_OPTIONS` 3732–3748; zone cascade 3552–3559; `TmMultiSelect` 3989–4012.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M4.1, M3.8

Packages: None

Shared infrastructure: FormGrid, FormActions, RepeatableRows, SegmentedRadio, SwatchRadio

Task-card additions: `FormGrid`, `FormActions`, `RepeatableRows`, `SegmentedRadio`, `SwatchRadio`.

## Files / Areas Expected

TBD at task start from the approved scope; do not create speculative files or empty folders.

## PR

TBD

## Review Findings

TBD

## Deviations

TBD

## Blockers

TBD

## Human Approval

Required

## Completion Notes

TBD

