# M1.1 — Scaffold and quality gates

Status: Completed

Owner: Codex

Reviewer: User

Depends On: M0.*

## Objective

Execute the M1.1 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- Vite + React 19 + TypeScript, using the Phase 2 §26 compiler flags and the `@/` alias.
  - `src/main.tsx` and `src/app/App.tsx` render an empty root.
  - ESLint flat config:
    - typescript-eslint (strict), react-hooks, jsx-a11y, and `no-explicit-any`;
    - `no-restricted-imports` boundary rules (Phase 2 §6, §30);
    - `no-restricted-syntax` rules flagging JSX text literals, physical-direction classes and default-palette classes.
  - Prettier.
  - Vitest + Testing Library + jsdom (`src/test/setup.ts`).
  - Scripts: `typecheck`, `lint`, `test`, `build`, `check:bundle`.
  - GitHub Actions running G1–G4 on every PR.
  - Empty register templates in `docs/migration/`.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

- All scripts green locally and in CI.
  - A scratch branch shows lint failing on a deep feature import, an `any`, a JSX literal and `pl-4`. The failures appear in the report; the branch is not merged.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): Q4, Q6

## Prototype Verification

Source: TBD where prototype verification is applicable.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M0.*

Packages: react, react-dom, typescript, vite, @vitejs/plugin-react, eslint, @eslint/js, typescript-eslint, eslint-plugin-react-hooks, eslint-plugin-jsx-a11y, prettier, vitest, jsdom, @testing-library/{react,user-event,jest-dom}

Shared infrastructure: lint/test/CI tooling, registers

Task-card additions: tooling only. No `src/` folder may exist without files.

## Files / Areas Expected

- Vite, TypeScript, ESLint, Prettier, Vitest, and package-manager configuration at the repository root.
- Empty React entry points under `src/`, plus the required test setup and one scaffold smoke test.
- `scripts/check-bundle.mjs` and `.github/workflows/quality-gates.yml`.
- Migration tracker and register updates for M1.1.

## PR

Not opened. The user explicitly requested no commit or push.

## Review Findings

- Local G1-G4 commands pass after a clean `npm ci`.
- A temporary uncommitted scratch branch proved lint failures for a deep feature import, explicit `any`, JSX text, and `pl-4`; the scratch file and branch were removed without merge.
- A temporary palette probe proved `bg-red-500` is rejected; the probe was removed.
- GitHub Actions is configured for G1-G4 on pull requests but was not run remotely because no commit, push, or PR was authorized.

## Deviations

None. `@types/node`, `@types/react`, and `@types/react-dom` are type-declaration companions required by the TypeScript scaffold.

## Blockers

No implementation blocker. Remote CI remains unexecuted until a future authorized commit/PR. The prior M0.1/M0.2 tracker records remain unchanged and require separate human review.

## Human Approval

Approved on 2026-09-25. The user confirmed that review was completed.

## Completion Notes

- R6: no M1.1 prototype source delta; `index.html` is unchanged from `273abc8`.
- G1: pass, `npm run typecheck` (0 errors).
- G2: pass, `npm run lint` (0 errors); required negative lint probes also passed.
- G3: pass, `npm run test` (1 file, 1 test).
- G4: pass, `npm run build` and `npm run check:bundle` (2 output files inspected).
- G5-G7: not active at M1.1.
- G8: pass; no shared or feature infrastructure was introduced.
- G9: pass; only M1.1 dependencies and required TypeScript declaration packages were added.
- G10: pass; register templates are present and the M1.1 delta intake is recorded. No NOOP, pending, or deviation entry was needed.
- Q4: npm with local Node.js `v22.15.0`.
- Q6: no preview deployment added.

