# M3.8 — SLA & Compliance

Status: Completed

Owner: Codex

Reviewer: User-approved on 2026-09-26

Depends On: M3.1

## Objective

Replace the `/quality/sla` migration marker with the prototype-faithful SLA & Compliance screen, complete the applicable Phase 3 gates, and stop for human approval.

## Scope

- `features/sla` at `/quality/sla`.
- Overview: monthly compliance, department table, department and item drill-ins, inert preview, time-used progress, and priority framework.
- Work-area mapping: exact fixture rows, search, priority filter, and Add/Edit/Remove behavior.
- Public hooks `useSlaLevels` and `useSlaPerformance`.
- First approved React Hook Form + Yup consumer and the required generic shared controls/adapters.

## Must Not

- Do not start M3.9 or change unrelated Quality & Compliance children.
- Do not modify the read-only prototype.
- Do not migrate dead UI or silently fix prototype behavior.
- Do not add API/server-state, Axios, TanStack Query/Table, chart, PDF, or competing form/validation infrastructure.

## Prototype Verification

Source inspected: `index.html` lines 21660-22070, including `saveEdit` around line 21820, plus directly used fixtures, navigation, Section/Report, theme, locale, department/item drill-in, and progress behavior.

- Prototype HEAD: `3c391b4`.
- Approved baseline: `273abc8`.
- Delta intake: the prototype `index.html` blob is identical at baseline and HEAD.
- Plan/prototype reconciliation: the source defines `clockCard` at line 21806, but the actual Overview return tree at lines 22058-22063 never renders it.
- Human decision on 2026-09-26: adopt current rendered behavior and omit the clock-rules card. The decision is recorded in `DECISIONS.md` and `PROTOTYPE_DELTAS.md`.
- Continued consistency check confirmed monthly compliance, department/table drill-ins, inert item preview, exact progress calculation, priority framework, mapping rows, Area-required silent save, empty-priority P3 default, and Add/Edit/Remove behavior.
- Section/Report uses the prototype fallback for `sla`: zero rows and the empty-preview message. No speculative SLA report definition was added.
- No second material plan/prototype conflict remains.

## Dependencies / Packages

- Dependencies: M3.1.
- Direct runtime additions:
  - `@hookform/resolvers@5.9.1`
  - `react-hook-form@7.89.0`
  - `yup@1.7.1`
- `package.json` ranges are `^5.9.1`, `^7.89.0`, and `^1.7.1`.
- Lockfile transitive additions are resolver/schema dependencies only: `@standard-schema/utils`, `property-expr`, `tiny-case`, `toposort`, and `type-fest`; npm also records the resolver's optional AJV peer and nests the existing ESLint AJV v6 copies.
- No unrelated direct package was added.
- `package.json` also adds only the `visual:capture:m3.8` script.

## Files / Areas Changed

- `src/features/sla/`: typed fixtures, feature types, pure domain rules, hooks, Overview/mapping/dialog components, page, public exports, and tests.
- `src/shared/ui/progress/`: generic accessible `ProgressBar` and unit test.
- `src/shared/form/controls/`: generic `TextInput`, `TextArea`, and shared-control test.
- `src/shared/form/rhf/`: only the consumed `RHFTextInput` and `RHFTextArea` adapters.
- `src/shared/form/schema/`: `yupLocale`, `requiredText`, and schema tests.
- `src/shared/i18n/locales/{en,ar}/sla.json`, validation resources, namespace registration/types, and parity test coverage.
- `src/app/router/router.tsx`, `src/app/navigation/nav.config.ts`, and Quality route regression coverage.
- `tests/visual/m3.8.spec.ts`, `package.json`, lockfile, and 55 side-by-side captures under `docs/migration/qa/M3.8/`.
- M3.8 decision, delta, NOOP, MigrationPending, task-status, and phase-report registers.

## Route and Section/Report Result

- `/quality/sla` now renders `SlaPage` under the existing M3.1 `SectionLayout`; its navigation node no longer carries pending metadata.
- `?view=report` reuses the shared fallback and matches the prototype's zero-row preview.
- Returning to Section restores the SLA screen.
- Other Quality routes keep their approved `PlaceholderPage` state under D14.
- The separate `/home/sla` future Home marker remains pending.

## Overview and Clock-Card Reconciliation

- Overview renders the actual prototype tree: This month summary, four priority performance cards, Department performance, and Priority framework.
- Totals are 316 work orders, 295 closed within SLA, 3/4 targets met, one at risk, and 93.4% overall.
- Priority values remain P1 100%, P2 93%, P3 92.8%, and P4 93.8%.
- The defined-but-unrendered clock-rules card is intentionally absent per the human reconciliation; tests and direct prototype evidence assert absence on both sides.

## Department Table and Drill-Ins

- All 11 department rows, columns, order, counts, response/resolution values, percentages, trend tones, keyboard row activation, and horizontal overflow are preserved.
- Department detail retains summary cards, priority breakdown, state/type filters, item cards, and the department note.
- Selecting an item opens the read-only item dialog with the exact record data, state/priority presentation, response/resolution/coverage values, progress bar, updates, Close action, overlay close, and Escape close.
- Dialog focus uses the existing shared overlay behavior; the Work-area dialog explicitly focuses Area without the inaccessible `autoFocus` attribute.

## Inert Preview and ProgressBar

- The item preview remains present but inert via `inert` and `data-prototype-noop="item-preview"`; no edit or navigation behavior was invented.
- The behavior is registered as `PROTOTYPE-NOOP(D2)` using prototype lines 21891-21894.
- Shared `ProgressBar` owns only clamped presentation, marker placement, accessible progress semantics, and RTL-safe rendering.
- SLA owns parsing and time-used semantics. It ports the prototype's 24-hour day conversion, rounded percentage, over-target cap, and overrun presentation.
- Tests cover 19/24 = 79%, 8/2 = 400% with a 100% fill cap, 2/7 days = 29%, and the zero-agreed boundary.

## Work-Area Mapping

- All 15 prototype mapping rows and their order/content are preserved.
- Search trims and lowercases the query and matches Area plus Conditional triggers only; it intentionally does not match response/resolution text.
- Filters are All, Critical/P1, High/P2, Medium/P3, and Low/P4, with All initially selected and exact multi-priority inclusion.
- Empty/no-results behavior and responsive horizontal overflow match the prototype.

## Add / Edit / Remove and Form Rules

- Add starts with empty Area/response/resolution/conditional fields and P3 selected.
- Edit initializes all existing values and preserves the mapping ID and list position on save.
- Remove is available only for an existing mapping and removes that row before closing.
- Valid saves trim text, mutate local feature state, and close the dialog.
- Area is required by a Yup schema built with generic `requiredText`.
- Missing/blank Area is a silent no-op: no mutation, no close, no validation text, toast, alert, or summary.
- If all priorities are deselected, an otherwise valid save succeeds and defaults to P3.
- The local `useSlaMappings` hook owns prototype-equivalent state; no global/server-state layer was added.

## React Hook Form, Yup, and Shared Form Infrastructure

- `WorkAreaDialog` is the first approved React Hook Form + Yup consumer.
- `shared/form/rhf` contains only the two adapters immediately consumed by M3.8.
- `TextInput` and `TextArea` follow existing token, focus, disabled, directionality, and native accessibility conventions.
- `yupLocale` is configured from the existing i18n instance at initialization and language changes.
- EN/AR validation resources have parity, while the M3.8 form intentionally suppresses visible Yup errors to preserve the prototype.

## Public Hooks and Domain Logic

- `useSlaLevels` exposes the four current prototype SLA levels.
- `useSlaPerformance` exposes performance, overall totals, department details/items, and the pure percentage function required by the current screen.
- `onTimePercentage` uses the prototype formula and one-decimal rounding; zero work orders returns zero.
- `overallPerformance` produces the rendered 295/316 and 93.4%.
- No speculative API client, cache, or future hook surface was added.

## EN/AR, RTL, Responsive, and Themes

- The SLA namespace is registered, typed, lazy-loaded consistently with existing feature namespaces, and covered by EN/AR parity tests.
- User-interface strings live in the owning namespace; prototype business fixture text that remains English in both locales stays feature data.
- Western digits are retained under D20.
- RTL reverses directional layout through existing logical properties while retaining usable tables, forms, filters, and dialogs.
- Paper and Ink were verified at 1440px; Paper EN was also verified at 760px and 390px; Arabic RTL was verified at 1440px.
- No mobile transformation absent from the prototype was invented; tables retain horizontal overflow and dialogs remain usable.

## Tests

- Focused M3.8/shared/form/route suite: 6 files, 29 tests passed.
- Domain tests cover on-time values and zero boundary; overall totals; time conversion/progress; missing Area no-op; P3 default; ID-preserving edit; search fields/case; and priority filtering.
- UI/form tests cover Overview values and clock-card absence, department/item dialogs and Escape, search/filter, valid Add/Edit/Remove, silent invalid save, empty-priority P3 default, route replacement, and shared primitives.
- Full regression: 55 files, 420 tests passed.

## Visual Evidence

- `npm.cmd run visual:capture:m3.8`: 56/56 passed after final regeneration.
- One direct oracle test compares normalized full department-table and mapping-table rows against the running prototype and asserts the clock card is absent from both.
- 55 side-by-side captures cover 11 states: Overview, department, item, mapping, search, priority filter, Add, Edit, invalid save, empty-priority result, and Report.
- Five variants per state: 1440 Paper EN, 1440 Ink EN, 1440 Paper AR, 760 Paper EN, and 390 Paper EN.
- Representative spot review covered the actual Overview tree, item preview/progress, Arabic mapping/RTL, mobile invalid-save dialog, empty-priority result, and Ink Report fallback.

## Quality-Gate Results

- G1 typecheck: pass — final `npm.cmd run typecheck`, 0 errors; the production build repeated TypeScript validation.
- G2 lint: pass — final `npm.cmd run lint`, 0 errors/warnings.
- G3 tests: pass — full Vitest suite 55 files / 420 tests; focused suite 6 files / 29 tests.
- G4 build/bundle: pass — production build transformed 2,205 modules; only the pre-existing >500 kB advisory. `check:bundle` passed with 27 files inspected.
- G5 i18n parity: pass — SLA and validation EN/AR resources are registered, typed, and included in parity coverage.
- G6 nav↔route parity: pass — `/quality/sla` is real, retains Section/Report behavior, and no longer resolves through `MigrationPending`; unrelated Quality children are unchanged.
- G7 fidelity evidence: pass — direct rendered-row oracle plus 55 regenerated captures; no open material M3.8 mismatch remains.
- G8 no speculative code: pass — every new shared/public export has an immediate consumer; no unsupported clock card, SLA report data, API/store, generic table, or extra form adapter was added.
- G9 dependency diff: pass — exactly the three approved direct dependencies were added at the recorded versions; no prohibited package is present.
- G10 registers: pass — decision/delta, NOOP, MigrationPending, task state, and phase report are updated; `report:pending` retains exactly the two expected shared future-phase markers.

## Regression Results

- Full Vitest suite: 55 files and 420 tests passed, including approved M1/M2/M3.1-M3.7 coverage.
- Production build and 27-file bundle inspection passed.
- `report:pending` reports exactly two markers: `MasterRoutePage.tsx:46` and `NavRoutePage.tsx:13`.
- `git diff --check` passed with line-ending notices only.
- No unrelated route changed, and the prototype repository was not modified.

## Register Updates

- `PROTOTYPE_DELTAS.md`: records the no-source-delta intake and human-approved clock-card reconciliation.
- `DECISIONS.md`: records adoption of the actual rendered Overview and omission of dead `clockCard` UI.
- `PROTOTYPE_NOOPS.md`: registers the inert item preview.
- `MIGRATION_PENDING.md`: marks only `/quality/sla` Removed/completed; `/home/sla` and future-phase markers remain active.
- Architecture deviation register: unchanged; no architecture deviation was required.
- `COMPLETED.md`: unchanged because M3.8 requires human approval before completion.

## Deviations

None. The approved clock-card reconciliation is a recorded plan/prototype decision, not an architecture deviation.

## Blockers

None. Remote CI/PR execution was not run because no commit, push, or PR was requested.

## PR

Not opened. No commit or push was performed.

## Human Approval

Approved by the user on 2026-09-26.

## User-visible behavior

1. `/quality/sla` now displays the real SLA & Compliance screen instead of `MigrationPending`.
2. Overview shows This month totals, four priority performance cards, the 11-row department table, and the priority framework; the unrendered prototype clock card is intentionally omitted.
3. Selecting a department opens its performance/detail dialog; selecting an item opens the read-only drill-in, which closes through Close, overlay, or Escape.
4. The progress bar represents parsed time used against agreed SLA time, with capped fill and visible overrun percentage/state.
5. Work-area search matches Area and Conditional triggers case-insensitively; priority chips filter rows by included SLA priority.
6. Add creates a trimmed local row, Edit preserves its ID/position, and Remove deletes an existing row, all through the shared dialog/form infrastructure.
7. Saving with missing Area does nothing silently: no row mutation, no dialog close, and no visible error/toast.
8. Saving valid data with no selected Priority succeeds and stores P3.
9. EN/AR, RTL, Paper/Ink, desktop/tablet/mobile, focus, keyboard, and horizontal-overflow behavior were verified.
10. No unrelated screen changed; other Quality children and `/home/sla` retain their approved future-phase state.

**M3.9 has not started.**
