# M8.1 — Task list

Status: Pending

Owner: Not assigned

Reviewer: Not assigned

Depends On: M7.1, M4.1

## Objective

Execute the M8.1 Phase 3 task exactly as defined, then stop for human approval.

## Scope

- `features/tasks` begins at `/home/work-centre/tasks`.
  - List, board and card views; settings dialog; RecordFilter `task`; internal/external kinds.
  - Table:
    - drag column reorder and grouping by status, priority, assignee or department;
    - clickable progress segments;
    - row actions: view → `/tasks/:id`, edit → `/tasks/:id/edit`, delete confirmation that hides the row client-side.
  - QA rows not editable on the board.
  - Seeded `tasks.store` plus `useTasks` and `useTask`.

## Must Not

- Do not exceed this task's Phase 3 scope or start the next task.
- Do not modify the read-only prototype.
- Do not silently fix prototype behavior or migrate excluded/dead code.
- Do not add packages or shared infrastructure beyond those listed for this task.

## Acceptance Criteria

parity for each view; grouping, ordering and delete-hide behaviors match.

## Quality Gates

Run every Phase 3 gate active and applicable at this phase (G1–G10), record the results, move the task to Review, and stop.
Specific gate(s): delta intake

## Prototype Verification

Source: `JobOrdersScreen`/`ListView` 16814–17278; `TasksTable` 16379–16513; `Board` 16665–16813; `TASKS` 15976 onward.

At task start, perform Phase 3 R6 delta intake against baseline `273abc8`. Use the Phase 3 QA matrix, behavior checklist, and evidence-pack requirements where applicable.

## Dependencies / Packages

Dependencies: M7.1, M4.1

Packages: None

Shared infrastructure: SegmentBar, column ordering/grouping

Task-card additions: `SegmentBar`; column ordering and grouping on the table engine.

## Files / Areas Expected

TBD at task start from the approved scope; do not create speculative files or empty folders.

## PR

TBD

## Review Findings

TBD

## Deviations

TBD

## Blockers

TBD

## Human Approval

Required

## Completion Notes

TBD

